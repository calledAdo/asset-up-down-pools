# Session 005 · 2026-06-23 · opus

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## CREATE redundancy trim in pool_type
status: done
intent: review pool_type CREATE for redundant checks and trim the ones that aren't load-bearing.
did: Removed two CREATE checks in `crates/up_down/contracts/pool_type/src/main.rs`
  `validate_create()`: (1) the `out.used_pt != 0` zero-state check, and (2) the
  `start_time <= now || close_time <= now` future-window check (and its `now_secs()` read).
  Kept `start_time < close_time` and `rake_bps <= RAKE_BPS_MAX`. Added comments explaining
  why each stays/goes. Updated the two tests that asserted the old behavior
  (`create_start_in_past_fails`/`create_close_in_past_fails` → `..._succeeds`). Updated
  `docs/pool_type-spec.md` §2 CREATE bullet. Rebuilt riscv contracts + ran host tests.
files: crates/up_down/contracts/pool_type/src/main.rs;
  crates/up_down/tests/src/create_tests.rs; docs/pool_type-spec.md
decisions: KEPT `rake_bps <= 10_000` despite user listing it for removal — it is load-bearing,
  not redundant: REDEEM computes `distributable = loser_total - floor(loser_total*rake_bps/10_000)`,
  so rake_bps>10_000 underflows u128; config_unchanged freezes rake_bps after CREATE, so this is
  the only catch point. Flagged this to the user rather than removing.
gotchas: tests must run via `make contracts-test` (host target + prebuilt riscv binaries);
  plain `cargo test` from the crate dir compiles for the wrong target and floods serde errors.
next: continue the per-transition redundancy review with DEPOSIT (validate_deposit in main.rs).

## DEPOSIT redundancy trim in pool_type
status: done
intent: review validate_deposit for redundant checks; user directed removing the header-clock gate.
did: Removed the `now >= prev.start_time` time gate (and its now_secs read) from
  `validate_deposit`. Rationale: deposits are bounded by the STATUS machine — a deposit only
  validates OPEN->OPEN, and ACTIVATE (OPEN->LOCKED) leaves OPEN the instant start_price is set,
  so "start price set" == "deposits closed". Now only CLOSE reads the header clock. Updated
  test `deposit_after_start_fails` -> `deposit_after_start_succeeds_while_open`. Updated
  docs/pool_type-spec.md (DEPOSIT bullet + the §2 "header clock" note). Kept everything else in
  DEPOSIT (underflow guards, funds conservation, share mint, 4-field state freeze — the freeze
  is consensus-redundant but kept to protect off-chain readers). 85 tests pass.
files: crates/up_down/contracts/pool_type/src/main.rs;
  crates/up_down/tests/src/deposit_tests.rs; docs/pool_type-spec.md
decisions: KEPT the start_price/settle_price/used_pt/winner freeze despite being provably
  overwritten/constrained by ACTIVATE in every branch — cheap, and keeps OPEN-phase PoolData
  honest for the watcher/indexer/SDK which read winner+prices.
gotchas: residual fairness window — deposits still possible in [start_time, ACTIVATE] while OPEN;
  relies on prompt keeper activation. Funds conserved regardless; not a drain.
next: continue with ACTIVATE (validate_activate) redundancy review.

## DEPOSIT redesign: invariant-based + withdrawal support
status: done
intent: per user, (1) drop redundant depositor_io check, (2) support withdrawal (totals may
  fall), (3) reframe funds/shares as absolute output invariants instead of signed deltas.
did: Rewrote validate_deposit in pool_type/src/main.rs:
  - Removed depositor_io (and its only helper sum_staked_asset) entirely — funding provenance
    is delegated to the staked asset's own xUDT type script (Σ asset in == Σ asset out).
  - Removed the "totals must increase / total>0" direction constraints -> OPEN->OPEN now covers
    deposit, withdraw, and rebalance.
  - Funds: xUDT now ABSOLUTE `treasury_out == next.up_total+next.down_total` (one visible
    treasury cell) + pool capacity unchanged; CKB uses unsigned no-subtraction delta
    `out_cap + prev_sum == in_cap + next_sum`.
  - Shares: per side unsigned form `side_out + prev.side_total == side_in + next.side_total`
    (works for mint AND burn). All sums via checked_add (wrap guards retained).
  Tests: removed xudt_deposit_wrong_asset_type_fails (it only exercised depositor_io; harness
  models asset as always-success so the real xUDT rejection can't be shown) — left an
  explanatory comment in its place. Fixed xudt_deposit_succeeds + (former) wrong-asset fixtures
  to be invariant-consistent (treasury 150->250, matching totals 150/250). 84 tests pass.
  Spec: rewrote DEPOSIT section as DEPOSIT/WITHDRAW with the invariant framing; removed the
  "WITHDRAW omitted in v1" note; added staked-asset xUDT to §3 delegated/out-of-scope.
files: crates/up_down/contracts/pool_type/src/main.rs;
  crates/up_down/tests/src/xudt_tests.rs; docs/pool_type-spec.md
decisions: pool_type now TRUSTS the staked asset to be a conservation-enforcing xUDT (documented
  in §3). Justified as GIGO — a non-conserving asset makes the pool worthless regardless, same
  class as a bad share_xudt_code_hash/oracle. Absolute treasury check is strictly stronger than
  the old delta (self-heals drift, ties funds directly to stored data).
gotchas: the xUDT test harness uses always-success as the asset type (no conservation), so any
  future test needing real asset-conservation behavior must vendor a real xUDT binary. The old
  xUDT deposit fixtures were internally inconsistent (treasury != totals) and only passed because
  the old check was delta-based; the new absolute check requires them to rhyme.
next: ACTIVATE (validate_activate) redundancy review — note it shares phase_frozen with
  RESOLVE/CORRECT/FINALIZE, so check those together.

## ACTIVATE redundancy review — NO CHANGES
status: done
intent: review validate_activate (+ phase_frozen, correct_start) for redundant checks.
did: Reviewed the whole price-phase family (ACTIVATE / CORRECT-start / RESOLVE / CORRECT-settle /
  FINALIZE) which share phase_frozen. Conclusion: ACTIVATE is already tight — no checks removed.
  I initially proposed collapsing ACTIVATE-VOID's `(one_sided && past_start) || past_close` to
  just `past_close`, arguing the DEPOSIT change (deposits open until activate) made the one-sided
  early-void premature/griefable. User corrected me and I agree: start_time IS the betting
  deadline and ACTIVATE fires ~at start_time (gap is seconds), so the early one-sided void is the
  correct prompt teardown. Crucially it's self-correcting: if the other side funded first, prev is
  two-sided -> one_sided=false AND mid-window past_close=false -> VOID condition fails -> pool must
  LOCK. So a filled pool can never be wrongly voided; a still-one-sided pool SHOULD void. The
  proposed collapse would be a REGRESSION (forces one-sided pools to wait until close_time to
  refund). phase_frozen is load-bearing (freezes totals/capacity/shares; xUDT treasury excluded).
files: (none — review only)
decisions: KEEP ACTIVATE-VOID condition as-is. KEEP the settle_price==0/winner==UNDECIDED anchors
  in LOCKED/CORRECT-start (defense-in-depth; RESOLVE-VOID copies settle_price forward so a clean
  LOCKED keeps VOID data honest). Optional-only: ACTIVATE-LOCKED and CORRECT-start could share a
  "set start tick" helper (cosmetic DRY, removes no checks) — not done.
gotchas: don't confuse start_time (config betting deadline, oracle-band lower bound) with the
  ACTIVATE tx time; they're ~equal by operational design, not by contract enforcement.
next: RESOLVE / CORRECT-settle / FINALIZE redundancy review (validate_resolve etc.).

## CORRECT-settle redundancy review — NO CHANGES
status: done
intent: review validate_correct_settle for redundant checks.
did: Reviewed. It's tight — nothing to remove. The one redundancy (a void_time band check) was
  ALREADY eliminated: used_pt < void_time always, so `pub < used_pt` implies `pub < void_time`
  (documented at main.rs:388-389). Lower bound `pub > close_time` is load-bearing (stops a
  corrector reaching a LOCKED/start-phase tick). start_price freeze is load-bearing & not covered
  by phase_frozen (winner_for depends on start_price -> could flip winner). winner must be bound,
  not derived. Same near-duplication as ACTIVATE/CORRECT-start: CORRECT-settle == RESOLVE-SETTLED
  except band upper bound (used_pt vs void_time). Across the 4 price-setting transitions there are
  2 shapes (set-start-tick, set-settle-tick) each appearing twice -> a DRY helper would collapse
  them but removes no checks.
files: (none — review only)
decisions: leave as-is pending user call on optional DRY refactor + cosmetic error-code nit
  (start_price-freeze returns ERROR_FUNDS_NOT_CONSERVED; ERROR_POOL_DATA_MALFORMED would read
  truer, in validate_resolve line 333/334 and validate_correct_settle line 381/382).
next: FINALIZE (validate_finalize) review — then REDEEM, CLOSE.

## share_xudt / treasury_lock / pool_admin_lock / common parsers review — CLEAN
status: done
intent: review the other 3 scripts + shared parsers for redundancies and security flaws.
did: Read all. ALL CLEAN, no changes.
  - share_xudt (90L): pool-present->0 (delegates supply to pool_type, sound because pool_type checks
    shares in EVERY pool-present tx; CREATE has pool in outputs so mint forbidden -> starts at 0).
    pool-absent->no-increase (burn ok, loser cleanup; forfeiting only hurts burner since payouts key
    off frozen winner_total). args[32] in {1,2} = cheap defense (keep). sum_amount guards len<16 +
    checked_add.
  - treasury_lock (53L): spendable iff input type_hash==pool_type_hash; only real pool satisfies it
    (typeID unique, unforgeable). Forces pool_type to run on every treasury spend.
  - pool_admin_lock (85L): continuation (group-wide, can't piggyback close of pool B on continue of
    A) or creator-escape (CLOSE only; pool_type still enforces status/grace/lock-continuity so no
    mid-game theft). Verified multi-pool-same-creator shared-lock-group case.
  - pool_data.rs from_bytes: bounds-safe (exact len 173/237 validated before direct indexing; early
    reads use checked get; variant constrained {0,1}). oracle_read.rs: exact 152 len. config_unchanged
    covers config-only fields.
  - Verified error-constant local copies (share_xudt/treasury_lock inline them for stable binary
    hash) match errors.rs: -1/-2/30/40. No drift.
  Non-issue noted: OracleRead.feed_id parsed but unused (find_oracle matches via type args; commit
  uses trust root) — harmless.
files: (none — review only)
decisions: whole contract suite sound. Security model = delegation web anchored on typeID uniqueness:
  pool_type is the single brain; share_xudt/treasury_lock defer when pool present; pool_admin_lock
  gates WHO not WHAT. No flaws, no exploitable redundancy.
next: contract review fully complete. Outstanding: user decision on withdrawal-window fairness edge.

## Full pool_type security + redundancy pass — CLEAN
status: done
intent: thorough end-to-end pass over pool_type for redundancies AND security flaws.
did: Read entire main.rs (entry/dispatch, load_pool, now_secs, type_id, all helpers, all
  transitions). Verdict: sound, no new redundancies, no flaws. Verified cross-cutting invariants:
  (1) pool_id continuity is free from the script-group mechanism (group = code_hash+args=typeID),
  so (1,1) input/output share pool_id; dispatch forbids batching/dup-typeID. (2) status machine
  complete & closed (no SETTLED->VOID, no un-lock, STATUS_CLOSED unreachable; tick==close_time ->
  only ACTIVATE-VOID). (3) arithmetic overflow-safe by construction: payout=x+profit not
  checked_add BUT safe because DEPOSIT's checked_add on next_total enforces up+down<=u128::MAX, and
  x<=winner_total, profit<=loser_total => x+profit<=up+down. All other subs guarded; mul_div_floor
  checked_mul. (4) treasury==totals holds inductively for xUDT (DEPOSIT checks output only; only
  DEPOSIT may touch treasury in OPEN; ACTIVATE forbids it; treasury_lock needs pool in inputs);
  ambiguous/2nd treasury can't steal (xUDT conservation + ambiguity reject). (5) REDEEM can't
  over-pay/hijack (exact payout equality; shares lock-protected; outside-burn only forfeits own
  claim). 84 tests green.
files: (none — review only)
decisions: FLAGGED one fairness edge (NOT a bug): withdrawal now lets a bettor EXIT on info in the
  [start_time, ACTIVATE] window (not just enter). Funds conserved; mitigated by prompt activation.
  Recommended: LEAVE it (window negligible, invariant-based code cleaner) — but user's conscious
  choice. Options: leave / disallow reduce-withdrawals / re-gate withdrawal behind now<start_time.
gotchas: this fairness edge depends on keeper activating promptly at start_time (operational, not
  enforced on-chain).
next: await user decision on the withdrawal-window fairness edge; otherwise pool_type is done.
  Could review share_xudt / treasury_lock / pool_admin_lock next.

## DRY refactor of price-setting transitions + error-code fix
status: done
intent: per user, (1) DRY the 4 price-setting transitions, (2) fix the start_price-freeze error
  code in RESOLVE/CORRECT-settle.
did: Added two helpers in pool_type/src/main.rs: set_start_tick(prev,next,&oracle,upper) and
  set_settle_tick(prev,next,&oracle,upper). set_start_tick does band (start<pub<upper) +
  settle==0/winner==UNDECIDED anchor + bind start_price/used_pt; upper=close_time for
  ACTIVATE-LOCKED, =used_pt for CORRECT-start. set_settle_tick does band (close<pub<upper) +
  bind settle_price/used_pt/winner(=winner_for); upper=void_time for RESOLVE-SETTLED, =used_pt for
  CORRECT-settle. Refactored all four callers to use them (caller keeps its own pre-step bits:
  ACTIVATE keeps the both-sides guard; RESOLVE/CORRECT-settle keep the start_price freeze before
  find_oracle). Removes no checks — pure DRY. Also fixed the cosmetic error code: start_price
  freeze now returns ERROR_POOL_DATA_MALFORMED (was ERROR_FUNDS_NOT_CONSERVED) in validate_resolve
  and validate_correct_settle, matching FINALIZE's pattern.
files: crates/up_down/contracts/pool_type/src/main.rs
decisions: helpers take &OracleRead + upper bound; start_price freeze stays in the callers because
  RESOLVE applies it across both SETTLED and VOID branches (can't fold into the settle helper).
gotchas: behavior-preserving — 84 tests still pass, build clean (no unused warnings).
next: pool_type fully reviewed + refactored. Optional: review share_xudt / treasury_lock /
  pool_admin_lock next if user wants.

## REDEEM + CLOSE redundancy review — NO CHANGES (pool_type review COMPLETE)
status: done
intent: review validate_redeem and validate_close for redundant checks.
did: REDEEM (main.rs:798-934): tight. State-freeze (6 fields) load-bearing (fixed payout ratio
  across partial redemptions). payout = treasury/capacity shrink, underflow-guarded. distributable
  = loser_total - rake safe due to CREATE rake_bps<=10000. ONE redundant check found:
  `winner_total == 0` (lines 914-916) — impossible for FINALIZED (both sides funded at LOCK, totals
  frozen) AND mul_div_floor already guards d==0. Recommend KEEP (cheap defense on payout division;
  invariant is 3 transitions away).
  CLOSE (main.rs:960-994): tight. status FINALIZED/VOID + now>close_time+close_grace (the ONLY
  remaining header-clock read) + shares_frozen + lock-delegated auth. ONE redundant check:
  shares_frozen (line 990) is defense-in-depth — pool is destroyed so any minted shares are
  unredeemable (no future PoolCell); comment admits it. Recommend KEEP (uniform supply-pinned
  invariant across all pool-consuming txs; removal buys nothing).
files: (none — review only)
decisions: keep both redundant checks (defense-in-depth, no removal upside). FULL pool_type review
  now complete: only CREATE + DEPOSIT had checks actually removed this session.
next: OPTIONAL cross-cutting work if user wants: (1) DRY refactor of the 4 price-setting
  transitions (set-start-tick/set-settle-tick helpers; removes no checks), (2) error-code nit
  (start_price freeze: ERROR_FUNDS_NOT_CONSERVED -> ERROR_POOL_DATA_MALFORMED in validate_resolve
  ~line 333 and validate_correct_settle ~line 381). Otherwise the redundancy review is done.

## FINALIZE redundancy review — NO CHANGES
status: done
intent: review validate_finalize (SETTLED->FINALIZED latch).
did: Reviewed. Tight, cleanest transition (no branch). All 4 price fields frozen (the latch;
  not covered by phase_frozen so required). pub>=void_time on AUTHENTICATED oracle time is the
  security crux — header clock would let an attacker finalize early and latch a wrong winner
  before a legit CORRECT-settle. Nothing removable. Confirms the error-code nit: FINALIZE freezes
  with ERROR_POOL_DATA_MALFORMED (correct), while RESOLVE/CORRECT-settle use
  ERROR_FUNDS_NOT_CONSERVED for the start_price freeze (the truer code is POOL_DATA_MALFORMED).
  Whole price phase (ACTIVATE->CORRECT-start->RESOLVE->CORRECT-settle->FINALIZE) now reviewed &
  tight; band redundancies already optimized out via used_pt.
files: (none — review only)
decisions: leave as-is; cross-cutting DRY refactor + error-code nit still pending user call.
next: REDEEM (validate_redeem) review, then CLOSE (validate_close).

## RESOLVE redundancy review — NO CHANGES
status: done
intent: review validate_resolve (LOCKED->SETTLED provisional / LOCKED->VOID no-resolution).
did: Reviewed. Tight, nothing to remove. start_price freeze (line 333) load-bearing & correctly
  hoisted over both branches (winner_for depends on it). SETTLED band close<pub<void_time both
  bounds load-bearing. VOID pub>=void_time authentic window-closed proof. VOID freezes
  settle_price/used_pt to prev -> this is WHY the LOCKED anchors (settle==0) are kept (copied
  forward). Correctly OMITTED: no two-sided re-check (LOCKED implies two-sided, totals frozen);
  tie -> winner VOID while staying SETTLED is intended (lets CORRECT-settle adjust). Same dup:
  RESOLVE-SETTLED~CORRECT-settle (band upper void_time vs used_pt); RESOLVE-VOID shares
  pub>=void_time with FINALIZE.
files: (none — review only)
decisions: leave as-is; cross-cutting DRY refactor + error-code nit still pending.
next: FINALIZE (validate_finalize) review.

## CORRECT-start redundancy review — NO CHANGES
status: done
intent: review validate_correct_start (LOCKED->LOCKED start-price contest, start<new_pub<used_pt).
did: Reviewed. Tight, nothing to remove. Band upper `pub < used_pt` is the monotone-down
  convergence (load-bearing; stops cherry-picking a later/favorable tick). Lower `pub > start_time`
  load-bearing (pub could be <= start while < used_pt). The `pub < close` bound is ALREADY
  optimized out — used_pt < close so pub < used_pt implies pub < close (same as CORRECT-settle's
  missing void_time check). settle_price==0/winner==UNDECIDED anchors kept (cheap defense;
  RESOLVE-VOID copies settle forward). Same set-start-tick shape as ACTIVATE-LOCKED (differs only
  in upper bound + ACTIVATE's both-sides guard).
files: (none — review only)
decisions: leave as-is; optional cross-cutting DRY refactor + error-code nit still pending.
next: FINALIZE (validate_finalize) review — then REDEEM, CLOSE.

## Deployment toolbox alignment (lean oracle format) — DOC NOTE ONLY
status: done
intent: per user "move to deployment, following the lean oracle deployment format"; scope chosen
  = "toolbox alignment only" (no broadcasts). Review deployment/ against lean_oracle/deployment,
  close gaps.
did: Side-by-side read of both toolboxes (index/deploy/codeDeploy/validate/config/ccc/build/
  artifacts/types + configs + .env.example). Conclusion: code-deploy/promote/validate layers are a
  faithful adaptation of the lean oracle format (data2 raw-blob, latestCandidate + numeric versions,
  promote canonicalize, validate:config preflight, devnet KnownScript override, devnet fee fallback);
  we go further with promote:* for all 4 families + validate:consistency. `npm run build` + 12/12
  tests pass. No committed stale artifacts (only devnet.*.json exist, gitignored, regenerated on
  redeploy; testnet/mainnet not yet deployed). Identified ONE structural gap: the oracle binding
  (oracle_commit inputs + oracle cell dep/header) has no canonical home in the deploy record — it is
  hand-authored in watcher LaneConfig.oracleIdentity + SDK presets/devnet.ts. Asked user how to
  handle it; user chose "leave it in the watcher" (deployment stays code-only). So no code change;
  made the scope boundary explicit in deployment/README.md (lean-oracle-format note + deliberate
  no-state-deployments divergence + oracle-binding-owned-by-watcher + decoupling: never import
  lean_oracle).
files: deployment/README.md (intro scope note added)
decisions: deployment toolbox stays CODE-ONLY by user decision; oracle binding is watcher-owned
  operational config, not a deployment artifact. No new config/artifact schema, no validate:oracle.
  Did NOT refresh devnet artifacts / SDK devnet.ts hashes (regenerate on next devnet broadcast; we
  are not broadcasting this block).
gotchas: SDK presets/devnet.ts pool_type v2 hash (0x64e2...) + devnet.pool-type.json are a snapshot
  behind HEAD (pool_type binary rebuilt this session). Harmless now; MUST re-deploy devnet + re-copy
  the snapshot before any devnet run that exercises the new CREATE/DEPOSIT code.
next: when ready to actually deploy: pick a target network (devnet e2e or testnet), bring up a live
  lean-oracle oracle cell first, then deploy:*/promote:* our 4 contracts, then wire watcher lane
  oracleIdentity + SDK preset from the fresh artifacts.
