# Session 001 · 2026-06-20 · opus

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Initialize timeline + decide frontend architecture
status: in_progress
intent: kickstart the frontend. First stand up the handoff record (no llmtimeline/ existed),
  then settle the CKB-dApp frontend architecture before scaffolding `packages/web`.
did: created `llmtimeline/state.md` + this session file capturing the whole-project state
  (contracts/sdk/watcher done & tested; frontend starting). Proposing the read/write-split
  architecture and confirming stack choices with the user.
files: llmtimeline/state.md, llmtimeline/sessions/001-2026-06-20-opus.md
decisions: frontend lives at `packages/web`, depends on `ckb-up-down-sdk` (file:../game-sdk);
  reads via watcher HTTP API, writes via game-sdk PlayerClient + CCC wallet signer; no
  frontend→watcher package import (HTTP contract only, per decoupling rule).
gotchas: watcher oracle is still deferred (StubOracleSource) — devnet pools stay OPEN until the
  real oracle lands, so full-lifecycle UI data needs the oracle or the mock-oracle test path.
next: get framework + wallet-connector decisions from the user, then scaffold `packages/web`
  (package.json, API client, CCC provider, pages, write flows).

## Pivot — no SDK in the browser; backend builds txs
status: in_progress
intent: user correction — NO project SDK (game-sdk/oracle) in the frontend. Frontend POSTs an
  intent to a backend endpoint that BUILDS the tx and returns it unsigned; frontend only signs
  + submits. Rework the write path accordingly. Saved as memory `frontend-no-sdk-rule`.
did: confirmed the mechanism by inspecting CCC: `SignerCkbScriptReadonly(client, lock)` inherits
  findCells/getAddressObjs from the base Signer, so the backend can run the SDK's
  `completeFeeAndChange` (which only does completeInputsByCapacity + completeFeeBy — no signing)
  with a key-less signer and return a FULLY-FORMED unsigned tx. Verified
  `ccc.hexFrom(tx.toBytes())` ↔ `ccc.Transaction.fromBytes(hex)` round-trips losslessly incl.
  witnesses. Wire format = molecule hex string.
files: (planned) watcher: src/api/txBuilder.ts (new), src/api/server.ts (POST /tx/* + CORS),
  src/service.ts (construct PlayerClient for the API). web: drop ckb-up-down-sdk dep; rewrite
  config.ts, wallet/useWallet.ts, api/client.ts, api/types.ts, tx/actions.ts, PoolDetailPage.tsx.
decisions: backend = the watcher API (already has game-sdk + config + client); it builds the
  COMPLETE unsigned tx (Option B) so the frontend is a thin sign+send shell. CCC versions:
  connector-react ^1.0.35 (independent of core ^1.12.x).
gotchas: CCC connector-react latest is 1.0.x NOT 1.12 — my first package.json pinned ^1.12.2 and
  npm install ETARGET-failed. Frontend keeps @ckb-ccc/* (wallet boundary) but NOT game-sdk.
next: implement watcher `src/api/txBuilder.ts` (buildDeposit/Redeem/Burn → hex via readonly
  signer + player.complete), wire POST routes + PlayerClient in server.ts/service.ts, then
  rework packages/web write path; npm install + typecheck both.

## Implement backend tx endpoints + SDK-free frontend
status: done
intent: realise the no-SDK design end to end — server builds txs, browser signs + submits.
did:
  WATCHER: new src/api/txBuilder.ts (createTxBuilder → deposit/redeem/burn; builds draft via
  PlayerClient, completes via SignerCkbScriptReadonly, returns ccc.hexFrom(tx.toBytes())).
  server.ts: ApiDeps.txBuilder?, CORS allows POST, POST /tx/{deposit,redeem,burn} with
  validation (poolId+lock required, deposit needs up|down>0, builder errors → 422, routes
  absent when no builder). service.ts: constructs a key-less PlayerClient on the keeper's
  client + createTxBuilder(feeRate). config.ts: optional WatcherConfig.feeRate. Added 5 POST
  fixtures (fake builder). build + 55 tests green.
  WEB: dropped ckb-up-down-sdk dep (CCC connector-react ^1.0.35 + core ^1.12.2 only). Rewrote
  config.ts (URL+network+RPC only), wallet/useWallet.ts (signer+lockHash+lock script, no
  PlayerClient), api/client.ts (+LockLike, +buildDeposit/Redeem/Burn POSTs), tx/actions.ts
  (POST intent → ccc.Transaction.fromBytes → signer.sendTransaction), PoolDetailPage.tsx
  (ctx={signer,lock}). tsc --noEmit clean; vite build clean (672 modules, 770 KB). grep
  confirms zero SDK/oracle imports in web/src.
files: packages/watcher/src/api/txBuilder.ts (new), .../api/server.ts, .../service.ts,
  .../config.ts, packages/watcher/tests/api.fixture.mjs; packages/web/{package.json,
  .env.example,README.md,src/config.ts,src/wallet/{WalletProvider,useWallet},src/api/client.ts,
  src/tx/actions.ts,src/pages/PoolDetailPage.tsx}.
decisions: backend returns the COMPLETE unsigned tx (readonly signer does input+fee+change), so
  the frontend's whole write path is `fromBytes → signer.sendTransaction`. Wire format = molecule
  hex string. Burn omits `sides` (backend defaults to both UP+DOWN).
gotchas: bundle is 770 KB (CCC) — fine for now, code-split later. Browser wallets generally
  can't reach a local devnet node, so live wallet testing wants testnet (blocked on a testnet
  deployment + SDK preset). Watcher oracle still deferred → devnet pools won't advance past OPEN.
next: live-wire web ↔ local watcher (indexer role) on devnet; smoke the /tx/* endpoints by
  signing a built tx with a private-key signer; then bundle/UX polish + testnet deployment.

## Frontend UX polish + runnable dev server
status: done
intent: make the app coherent + presentable before live data; make `npm run dev` show something
  sensible even with no backend.
did: added shared async-state primitives (ui/states.tsx: Skeleton/EmptyState/ErrorState) and a
  BackendBanner (ui/BackendBanner.tsx + useHealth hook) that warns when the watcher is
  unreachable so empty pages read as "backend down". Applied states across all 4 pages; added
  page titles, a back link + footer. Route-level code splitting (React.lazy + Suspense) and
  vite manualChunks (ccc/react/query vendors): app shell now 10 KB, pages 1–5 KB lazy, CCC
  isolated at 553 KB in its own cached chunk; chunkSizeWarningLimit 700 → clean build. tsc clean,
  vite build clean (no warnings). Dev server verified: `npm run dev` → http://localhost:5173
  serves 200.
files: packages/web/{vite.config.ts, src/App.tsx, src/api/hooks.ts(+useHealth),
  src/ui/states.tsx(new), src/ui/BackendBanner.tsx(new), src/styles.css,
  src/pages/{LanesPage,PoolDetailPage,PositionsPage,HistoryPage}.tsx}.
decisions: keep CCC as one vendor chunk (can't shrink); graceful degradation over a hard error
  when the backend is down. Dev server left running in background (id b32wjdync).
gotchas: with no watcher running, the app shows the backend banner + empty states by design —
  that's correct, not a bug. Real data needs the watcher (indexer role) on VITE_WATCHER_API_URL.
next: run the watcher (indexer) against devnet to populate Markets; then testnet deploy for a
  real browser-wallet deposit round-trip.
