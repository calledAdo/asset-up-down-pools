# Session 004 · 2026-06-22 · opus

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Markets design critique + lifecycle/density rework
status: done
intent: pick up Codex's pending "featured-card density revision". Critique the current Markets
  design and improve it. Concrete problems: (1) the 456px featured square wastes ~60% of the row
  and only duplicates a grid card at 3× size — reads empty; (2) "UP CHANCE" overstates certainty;
  (3) no lifecycle separation (open vs locked/in-play vs settled); (4) heavy chrome (two chip rails
  + big square) before any market.
did: removed the FeaturedMarket square entirely. Markets now lead with a dense **market-stats strip**
  (Open rounds · Total liquidity · Next-lock countdown), then asset + cadence filters, then lifecycle
  sections: **Open markets** (the existing actionable grid; the single highest-liquidity open lane —
  via `featuredLane()` — gets a small "Most liquidity" corner tag instead of a giant square) and
  **In play** (locked rounds: frozen, dimmed split, "settles <countdown>", an "In play" pill, and NO
  buy buttons — whole card links to detail). Re-worded the headline % from "UP CHANCE" to just "UP"
  with a one-line section legend "% = share of the pool on each side". Mock now emits 3 `locked`
  pools and `/pools` honours `?status=`; detail route includes locked pools. Markets fetches In-play
  via `usePools({status:"locked"})`.
files: packages/web/src/pages/LanesPage.tsx (rewrite), src/styles.css (dropped featured-market/fm-*/
  market-board/market-scope; added .market-stats/.stat, .section-head, .mc-tag, .market-card.in-play,
  .pill.locked; .market-card now position:relative), mock/server.mjs (+LOCKED/lockedPools, /pools
  status filter, detail includes locked).
decisions: critique stance — a 456px square is the wrong device for an asset×cadence matrix product;
  density + lifecycle structure beats one huge number. Kept marketFilters.ts helpers (featuredLane
  repurposed for the tag, not a separate hero). Did NOT add per-asset section headers (asset chips
  already scope); lifecycle (Open vs In play) is the headline IA win. Settled stays on History.
gotchas: foreground `sleep` is blocked in this env (exit 144) — start the mock via run_in_background
  and poll with `curl --retry`, don't `sleep`. The mock is dev-only; the real watcher already has
  `/pools?status=` so the In-play wiring maps to it 1:1.
verify: `npm run typecheck` clean, `npm run build` clean, `node tests/marketFilters.test.mjs` exit 0,
  grep shows no dangling featured/fm/board refs. Live headless screenshots: /tmp/rework-markets.png
  (full) and /tmp/rework-top.png (stats strip + cards) — stats strip ticks, In-play shows 3 locked
  rounds with settle countdowns + no buy buttons. Mock (:8080) + dev (:5173) running for review.
next: lifecycle could extend to a richer In-play treatment (last price vs price-to-beat, win-prob
  shading); and the real watcher swap. See state.md Next.

## Lifecycle-correct chart + featured hero restored (full-width, dense)
status: done
intent: two user corrections. (1) An OPEN market must NOT show a "price to beat" line — the start
  price is captured at LOCK, so by the time it exists the round is no longer open. (2) User wanted
  the big featured frame back (I'd removed it) — but as a dense "big-picture frame" for the
  highest-liquidity OPEN market, not the empty 456px square.
did:
  LIFECYCLE-CORRECT CHART: mock `withSeries` no longer stamps a start price on OPEN pools — they
  keep `prices.start="0"`, so the detail chart reads as recent/spot context. `PriceChart` draws the
  dashed price-to-beat line + label ONLY when `priceToBeat > 0` (locked/settled); for open it colours
  the last marker by the window's net direction. `ChartHead` is lifecycle-aware: OPEN → "Spot price"
  + "Locks in <countdown>" (no beat); LOCKED → "Last price" + "<delta>% vs beat" + "Price to beat";
  SETTLED → "Settle price". Added `ChartNote` explaining each state ("…price to beat is captured when
  this round locks." / "Live since lock. UP wins if settle finishes above the beat." / settled).
  FEATURED HERO (restored, full-width, dense): `FeaturedHero` in LanesPage = the highest-liquidity
  OPEN lane. Left = identity + big UP/DOWN % + tug bar + Buy UP/Buy DOWN + liquidity/live/rake; right
  = "Spot <asset>" price + a `Sparkline` (new `src/ui/Sparkline.tsx`, line+area, no beat) + the note
  "price to beat is set when the round locks". Pulls the series via `usePool(featuredPoolId)`. Dropped
  the 3-tile MarketStats strip (replaced by the hero; liquidity folded into the Open-markets section
  head). Featured lane excluded from the grid; removed the now-redundant per-card "Most liquidity" tag.
files: mock/server.mjs (withSeries), src/ui/PriceChart.tsx (optional beat line), src/ui/Sparkline.tsx
  (new), src/pages/PoolDetailPage.tsx (ChartHead lifecycle + ChartNote), src/pages/LanesPage.tsx
  (FeaturedHero, drop MarketStats/tag, liquidity in section head), src/styles.css (.hero-*, .spark*,
  .chart-note; removed .market-stats/.stat/.mc-tag).
decisions: the hero is an OPEN market, so it uses a spot sparkline (no beat) — consistent with the
  detail rule. Real watcher already won't set a start price until activate, so this maps 1:1.
gotchas: foreground `pkill -f "mock/server"` MATCHES ITS OWN SHELL (the pattern is in the command
  line) → kills itself with exit 144. Use `pkill -f '[m]ock/server'` (char-class trick) or kill via
  the background task id. Same family as the blocked-`sleep` 144 seen earlier.
verify: tsc clean, build clean, `tests/marketFilters.test.mjs` exit 0, grep clean (only a header
  comment updated). Live screenshots: /tmp/v2-markets.png (hero), /tmp/v2-open.png (OPEN: spot, NO
  beat line, context note), /tmp/v2-locked.png (LOCKED: beat line at 64,180.2, "-0.44% vs beat").
  Mock restarted (bg) on :8080; dev on :5173.
next: a richer In-play card (spot vs beat shading) and the real-watcher swap remain (state.md Next).

## Hero v3 — featured ASSET with stacked cadence rounds (Polymarket/Kalshi pattern)
status: done
intent: user said the single-round featured frame still looked "vague". Studied Polymarket (Kalshi
  429'd, sportspredict is a JS SPA — used Polymarket + prior knowledge). Key learning: a featured
  tile earns its size by holding MANY sub-markets under one subject (Polymarket: one match →
  moneyline + spreads + totals + volume + time), not by enlarging one number.
did: our analogue = one ASSET, many cadences. Rebuilt the hero as `AssetHero`: header = asset icon +
  "BTC/USD" + "N open rounds · <liquidity> CKB" + right-aligned Spot price + %change + Sparkline;
  body = a stack of `CadenceRow`s (1m/5m/15m/1h/4h/1d), each row = cadence label + per-round volume +
  "locks <countdown>" + UP% + tug bar + DOWN% + Buy UP/Buy DOWN (multiples). Featured asset chosen by
  summed open liquidity (`featuredAsset()` in marketFilters.ts). The OTHER assets drop to a "More
  markets" card grid; "In play" unchanged. No price-to-beat anywhere here (all open). Removed the
  old single-round `.hero-*` CSS; added `.asset-hero/.ah-*/.cad-*` (kept `.spark`).
files: src/pages/marketFilters.ts (+featuredAsset), src/pages/LanesPage.tsx (AssetHero + CadenceRow,
  restructure: filters → asset hero → More markets → In play), src/styles.css (asset-hero/cad-row).
decisions: feature the asset, not a round — density via many actionable rows. Single-asset filter →
  that asset is the hero and "More markets" is empty (hidden). featuredLane() kept in marketFilters
  (still used by its test) but no longer imported by the page.
gotchas: tsc `noUnusedParameters` is ON — dropped the unused `asset` prop from AssetHero (derive the
  label from lanes[0]). Same env quirks as before (no foreground sleep; `pkill -f mock/server` self-
  matches — use `[m]ock/server`).
verify: tsc + build clean. Live screenshots /tmp/v3-markets.png + /tmp/v3-hero.png — hero shows ₿
  BTC/USD, spot 64,149.78 +0.18% + sparkline, 6 cadence rows each with vol/countdown/odds/buys; ETH
  in "More markets"; In play below. Mock (:8080) + dev (:5173) running.
next: optional — emphasize the most-liquid row, add a tiny per-row spark, Daylight pass; real-watcher swap.

## Top hero v4 — PROMOTIONAL banner carousel (not a market tile)
status: done
intent: user clarified the "big square" should be a PROMOTIONAL platform — a picture/banner slider
  (new listings, events, how-it-works) like SportsPredict's home carousel — NOT market data. So the
  market-tile hero (v3 asset hero) was the wrong thing in that slot.
did: new `src/ui/PromoSlider.tsx` — a banner carousel at the very top: a `promo-track` of full-width
  slides (translateX, 0.55s ease), auto-advances every 6s (skipped under prefers-reduced-motion),
  prev/next arrows + clickable dots. 3 placeholder slides (eyebrow + title + subtitle + white CTA)
  on themed gradients (primary / up / down via color-mix); each supports a drop-in `image` (renders
  as background) and shows a dashed "Banner image" placeholder tag until then. LanesPage reverted to:
  PromoSlider → filters → "Open markets" CARD GRID (all open lanes, sorted by lock) → "In play".
  Removed the v3 AssetHero/CadenceRow from the page. CSS: replaced `.asset-hero/.ah-*/.cad-*` with
  `.promo*`; kept `.spark` (+ Sparkline.tsx) for reuse though now unused.
files: src/ui/PromoSlider.tsx (new), src/pages/LanesPage.tsx (PromoSlider + grid markets),
  src/styles.css (.promo*). Orphaned-but-harmless: marketFilters.featuredAsset (still exported/tested),
  src/ui/Sparkline.tsx (no longer imported).
decisions: top slot = marketing surface (promos/events/new listings), markets = the grid below —
  matches SportsPredict/Polymarket. Slides are real banners now (gradient placeholders), swap to
  images by setting `image` per slide; copy is editable in PromoSlider's SLIDES array.
verify: tsc + build clean. Live screenshots /tmp/v4-markets.png + /tmp/v4-promo.png — blue "ETH/USD
  is live" promo with CTA, "Banner image" tag, arrows + dots; markets grid + In play below. Mock
  (:8080) + dev (:5173) running.
next: wire real banner images + real promo targets when available; optional Daylight pass; the
  real-watcher swap remains the big dependency.

## Week 3 report + stage deployment for git
status: done
intent: user wants a Week-3 report under `reports/` covering ONLY the deployment-folder work, then
  `deployment/` staged for git, while `llmtimeline/` and `packages/` stay UNSTAGED for now.
did: wrote `reports/week3-june-2026-update-report.md` (matches week1/week2 format; scoped strictly to
  the `deployment/` toolbox — code-deploy of the 4 binaries, candidate-vs-canonical artifacts +
  promote, data2 script identity, pool_type↔script-selection decoupling + game-sdk decoupling, pool
  creation out of scope, dry-run/preflight safety, artifact tracking policy, tests). Staged via
  explicit paths: `git add deployment reports/week3-june-2026-update-report.md`. Confirmed staged set
  = 6 deployment files (README, package.json, src/{config,index,types,validate}.ts) + the report;
  node_modules/dist/devnet-artifacts excluded by .gitignore. Verified `llmtimeline/` and `packages/`
  are NOT staged.
decisions: STAGED only (git add), did NOT commit — user said "add", not commit, and is clearly
  curating the next commit; leaving the commit to them.
next: commit when the user confirms message/scope.
