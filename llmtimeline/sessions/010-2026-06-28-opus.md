# Session 010 · 2026-06-28 · opus

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Ingest + watcher understanding
status: done
intent: pick up cold, read the timeline + memory, then understand the current watcher.
did: read state.md (s009) + watcher memory + full packages/watcher/src. Found the code has
  advanced past the timeline: a complete oracle subsystem now exists (oracle/{source,worker,
  liveSource,leanSource,leanNetwork}.ts), plus mutex.ts/odds.ts/actions.ts and lean-oracle-sdk
  as a file: dep. So "phase 7 — wire real OracleSource" is substantially BUILT, not pending.
files: (read-only)
decisions: none — orientation only.
gotchas: state.md is stale on the oracle (still describes StubOracleSource as the v1 default).
next: redesign discussion (below).

## Keeper redesign — design doc
status: done
intent: design a clean-slate keeper (no back-compat) per the user's edge-triggered, self-
  scheduling model, iterated over several rounds, then write it up.
did: wrote docs/keeper-redesign.md. Converged model:
  - Edge-triggered self-scheduling (replaces level-triggered plan() sweep) + a low-freq
    safety sweep backstop (user: include it).
  - Wake entries are revisit TOKENS (create:cadence | pool:poolId); the concrete action is
    decided at fire time from fresh state — NOT stored.
  - Decision = pure classify(state,time) + pure decide(pool,now,T) where T = the oracle
    cell's CURRENT tick. Full decision table incl. corrections + void + close.
  - Corrections (CORRECT-start/settle) are OPPORTUNISTIC: keeper reads the current on-chain
    cell, acts only if its tick is in (lower, used_pt); else leaves it. Always converge when
    the cell allows; if published beyond, ignore. (User decision #1/#2.) => NO historical
    zero-init mint needed; keeper is a PURE oracle reader. Worker owns liveliness+correctness.
  - VOID is a fallback branch (one-sided / window-missed / past-void) — also how downtime
    catch-up resolves.
  - Oracle seam slims to readCurrentTick(feedId); one read per FEED per bucket → shared
    CellDep → natural batching (group by oracle cell; find_oracle forbids 2 same-feed deps).
  - Scheduler (Timeline): slots Map<dueTime,{timerId,entries}> + poolIndex + cadenceIndex
    (one outstanding wake per pool/cadence). CLOSE = removePool (de-register, no re-arm) —
    the only action that shrinks the schedule. Move = remove-old+insert-new.
  - Wake serialization (user asked what it means): single-flight run-queue for onWake bodies
    so interleaved async handlers can't corrupt registries / double-spend; wallet Mutex stays
    as inner guard (shared with oracle worker in `all` mode).
  - Wind-down is EXTERNALLY managed (user decision): keeper just exposes enable/disableCreating;
    existing pools drain naturally; re-enable re-seeds creates.
files: docs/keeper-redesign.md (new)
decisions: see above — all confirmed with the user across the thread.
gotchas: NEW DEPENDENCIES flagged in the doc §13 — SDK keeper-client needs draftCorrectStart/
  draftCorrectSettle/draftVoid (+ batch support); these don't exist yet (executor.ts today only
  has activate/resolve/finalize/close/create + draftTransitionBatch). Oracle worker must hold
  canonical first-ticks at boundaries for the read-only keeper to work.
next: if user approves, implement — start with Cadence + Timeline + classify/decide (pure,
  unit-test the whole status×time×tick matrix), then Keeper, then the SDK correction/void
  builders, then swap service.ts keeper wiring. Replace planner.ts; keep executor/reconcile/
  mutex/config helpers.

## Create-handler grid fix (design refinement)
status: done
intent: user noted a CREATE should schedule TWO follow-ups at the next cadence boundary (the
  new pool's activate + the next create). Validate and fix the doc.
did: confirmed + corrected an off-by-one in docs/keeper-redesign.md. Grid: a round startTime=S
  has price window [S,S+d] and deposit window [S−d,S] (FULL duration). At each boundary S:
  resolve(start=S−d) + activate(start=S) + create(start=S+d). So a CREATE keyed to boundary B
  mints startTime S=B+d and schedules (a) that pool's ACTIVATE at S and (b) the next CREATE
  keyed to S — both at the next boundary, as the user said. My doc had startTime=boundary with
  create firing at boundary−lead, which gave a deposit window of only `lead` seconds (bug);
  fixed to full-duration. Also clarified timer offsets: pool/oracle wakes fire at
  boundary+postBoundaryDelay; create wakes fire at boundary−createLead (so the two follow-ups
  sit in adjacent slots bracketing S — harmless since CREATE is its own tx, not oracle-batched;
  ACTIVATE still batches with the closing round's RESOLVE at S+delay).
files: docs/keeper-redesign.md (§6 timer offsets, §10 create walkthrough + start() step 2)
decisions: keep create as a separate (non-batched) tx; pre-stage by createLead; full-duration
  deposit windows.
next: unchanged — awaiting go to implement.

## Cross-cadence batching + one-cell-per-feed reconciliation + restart sequence
status: done
intent: user asked (a) does the design batch different-cadence same-feed actions in one tx
  (e.g. 5m resolve + 1h activate)? (b) tie create to the wind-down flag, (c) detail the
  restart sequence.
did:
  - Confirmed YES (a): bucket coalesces by timestamp across cadences; executor groups by oracle
    CELL; same-feed coincident transitions need the SAME first-tick≥boundary so they share one
    CellDep → one tx. Found + fixed a doc contradiction: timing-spec §2 said "one cell per LANE"
    (would break batching via find_oracle's same-feed-dep rejection), but the implemented
    OracleWorker is already per-FEED. Updated timing-spec §2 → "one cell per feed" (+ contention
    rationale + 5m/1h example); added a "Cross-cadence batching" subsection to keeper §5. Cross-
    FEED (BTC+ETH) never batches (different cells).
  - (b) Renamed the flag to `windingDown` (off by default on start); gates creation in TWO
    places — start() step-4 seed AND the create-wake handler. Rewrote §11; updated the Keeper
    skeleton (`setWindingDown(on)`).
  - (c) Rewrote §10 start() as the explicit restart sequence (same path for crash or planned
    stop, since the schedule is never persisted — only chain + tx_log are durable):
    1 reconcile tx_log FIRST (honest guards) → 2 listOwnPools from chain → 3 schedule each pool
    (catch-up cascade voids/finalizes/closes anything missed) → 4 seed creates iff !windingDown
    → 5 arm safety sweep. Idempotency (tx_log + on-chain status) makes re-fire harmless. Fixed
    an ordering bug (seed-create must come AFTER listing pools, since "ensure current round
    exists" needs the pool list).
files: docs/timing-spec.md (§2), docs/keeper-redesign.md (§5, §10, §11, Keeper skeleton)
decisions: ONE ORACLE CELL PER FEED (shared across cadences) — enables cross-cadence batching;
  matches the built worker; timing-spec §2 updated. windingDown default OFF on start.
gotchas: timing-spec §2 still says the game deploys its OWN oracle cell, but bin/server.ts
  currently uses network.deployment.defaultPublicOracleLock (the SHARED public cell) — a
  separate divergence (operational, the cell-dep liveness race §2 warns of), NOT reconciled
  here; flag for later.
next: unchanged — awaiting go to implement.

## Operator-supplied cadence anchoring (firstCreateAt)
status: done
intent: user: the grid must NOT be epoch-aligned — the operator supplies, per cadence, the
  first-create timestamp at startup (e.g. 1d pool anchored to 12:00 noon, 5m at startup+3min).
did: added `firstCreateAt` per Cadence; grid = firstCreateAt + k·duration (phased, not epoch).
  Split start() step 4 into COLD (no live pools → use operator firstCreateAt, may be future) vs
  WARM (live pools → continue THEIR grid, ignore the anchor) so a restart can't fork a cadence
  onto a misaligned grid; re-anchoring a live cadence requires draining first. Added a "Cadence
  anchoring" subsection to §10 and updated the §13 Cadence bullet (nextBoundary/roundStartFor
  phased to firstCreateAt).
files: docs/keeper-redesign.md (§10 step 4 + new subsection, §13 Cadence)
decisions: firstCreateAt anchors COLD cadences only; WARM restart continues existing-pool phase
  for stability.
next: unchanged — awaiting go to implement.

## Unify startup with the steady-state wake path (no inline catch-up)
status: done
intent: user: after listPools, run the SAME classify the normal wake uses; don't fire inline at
  startup — schedule due actions a short delay out (~2 min). Collapse startup/crash/wind-down
  onto the one steady-state path to shrink code surface.
did: introduced a single rule `nextWakeTime(pool, now)` = future boundary (+postBoundaryDelay)
  if not yet due, else `now + catchUpDelay` ("soon", never instant) — used at startup, post-
  action, and steady state alike. Invariant added: **start() only reconciles/reads/schedules;
  ALL execution flows through the serialized wake handler (§9)** — no startup-specific catch-up
  code. Reframed start() step 3 (schedulePool via nextWakeTime; overdue→now+catchUpDelay; all
  overdue pools share one `now` → coalesce+batch) and step 4 (seed ONE create wake per cadence;
  the create handler is now idempotent — skips mint if the round exists — and self-perpetuates;
  cold/warm only picks the grid phase). Made the create-wake idempotent (§10) and reframed the
  pool-wake post-exec re-arm to use nextWakeTime (catch-up cascade = one action per soon-wake).
  Noted 3 side benefits: batched recovery, lets the permissionless market act first (saves gas),
  cleaner crash-safety (start() can't half-fire txs).
files: docs/keeper-redesign.md (§3 nextWakeTime rule, §10 start() preamble + steps 3/4, create-
  wake idempotency, pool-wake step 5)
decisions: `catchUpDelay` is one small tunable, shared with the skip/retry delay (user's 2 min =
  conservative end; seconds–tens-of-seconds keeps recovery within a short cadence).
next: unchanged — awaiting go to implement. The pure surface to build is now just: classify /
  decide / nextWakeTime (all pure) + the one wake handler + Timeline + Cadence.

## Registry stores the predicted action; wake = validate-or-replan
status: done
intent: user: store the actual recommended action in the registry entry; on wake, validate that
  stored action against current state — perform if still right, else perform nothing and re-plan
  the next (action, when). Answers "how does the crash wake schedule the next tx" (the wake's
  OUTPUT is the next {action, at}).
did: pool WakeEntry now carries `action: ActionKind` (the predicted tx for that time). Defined
  two pure read-outs of the classify core: `decide(pool, now, tick)` (what's valid NOW — the
  fire-time validator) and `next(pool, now) -> {action, at}` (the next action + when; `at` via
  the future-boundary-else-now+catchUpDelay rule). Rewrote the pool-wake handler to: readPool →
  read tick → VALIDATE stored action (still what decide would do?) → execute if valid else do
  nothing → arm next {action, at} (re-read post-state). schedulePool gains the action arg.
  Updated §2 (WakeEntry + hint-not-commitment paragraph), §3 (re-arm = next(); decide vs next),
  §6 (schedulePool sig), §10 (start step 3, create-wake schedulePool("activate"), pool-wake fires).
files: docs/keeper-redesign.md (§2, §3, §6, §10)
decisions: each wake is bound to ONE predicted action; on divergence it re-plans (defers) rather
  than firing a different action inline — consistent with the don't-rush/let-market-act stance;
  costs at most one catchUpDelay hop. Registry is now self-describing (shows planned action/pool).
next: unchanged — awaiting go to implement.

## REVERT: drop catchUpDelay + stored action; immediate overdue firing; batched recovery
status: done
intent: user reconsidered the prior two turns. New principle: "perform now if overdue-but-undone,
  else schedule to the actual due time" — removes the artificial ~2min catchUpDelay (overdue fires
  ~immediately). And since the action is derived from state at fire time, the stored action hint is
  redundant → drop it. Then user pushed back on my "lose let-market-act-first" trade-off: it's a
  non-issue because due-but-undone actions still BATCH (not one at a time).
did: reverted both prior refinements.
  - nextWakeTime(pool, now): future boundary (+postBoundaryDelay) else **now** (no catchUpDelay).
    Kept a `retryDelay` ONLY as a backoff when the keeper couldn't act (oracle cell behind →
    decide null while past boundary, or tx failed) — avoids a hot spin loop; NOT a recovery delay.
  - WakeEntry pool variant back to bare `{poolId}`; action derived at fire time via decide().
    schedulePool(poolId, dueTime) (dropped action arg).
  - Pool-wake handler reverted to: readPool → readTick → decide → execute-if-action → re-arm
    (now if next overdue / boundary if future / now+retryDelay if couldn't act).
  - Corrected the trade-off: "let market act first" is NOT lost — decide() at fire time returns
    null if the market/sibling already did it (no gas), independent of any delay. And recovery
    BATCHES: start() arms all overdue at one shared `now` → one bucket → handler reads tick once,
    decides each, executor groups by oracle cell → ONE tx (mixed kinds OK; shared oracle dep).
    Multi-step pools advance in batched waves. Serialization is between WAKES not within (a wake
    batches its whole bucket). Added executor batch-size CAP + CHUNKING for large recovery buckets.
files: docs/keeper-redesign.md (§2 WakeEntry+para, §3 re-arm intro + nextWakeTime note, §5 new
  "Recovery batches identically" subsection, §6 schedulePool sig, §9 between-wakes note, §10 start
  step3 + step4 + create-wake + pool-wake handler, §13 executor chunking dep)
decisions: NO catchUpDelay (immediate overdue). NO stored action (derive at fire time). retryDelay
  kept solely as oracle-not-ready/tx-failed backoff. Recovery reuses the steady-state batch path +
  chunking; no separate healing mechanism.
gotchas: net effect reverts sessions-010 turns "store predicted action" and "catchUpDelay soon-
  delay". The mixed-kind batch (activate/resolve/finalize/void/correct in one tx) depends on the
  SDK draftTransitionBatch covering the new kinds (already a §4 dep) + the new executor chunking.
next: unchanged — awaiting go to implement.
