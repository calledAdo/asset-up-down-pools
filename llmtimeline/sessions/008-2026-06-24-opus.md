# Session 008 · 2026-06-24 · opus

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Watcher / backend architecture think + cross-agent compare → CONVERGED DECISION
status: done
intent: user asked to ignore current structure and propose a backend architecture from scratch,
  accounting for multi-asset (BTC/ETH/SOL), multi-cadence (15m/1h/1d...), continually-repeating
  actions, and how oracles are partitioned + periodically updated. Design discussion, no code.
did: delivered a three-plane proposal — (1) CONTROL/keeper plane partitioned BY WALLET, driven by
  a deterministic timer wheel (not polling) since the grid makes all transition times computable;
  reads chain as truth. (2) ORACLE plane partitioned BY FEED ONLY (durations multiplex one price
  stream → cost is O(feeds×cadence) not O(lanes×rounds)); ingester→publisher(1 writer/cell)→read
  adapter. (3) READ plane = single indexer + REST + unsigned-tx builders. Key claims surfaced:
  oracle publish_time is the authoritative clock for price phases (chain time only gates teardown);
  oracle lag is fail-safe (VOID/refund, never mis-settle); sliding-window of active rounds;
  idempotency via on-chain status + tx_log + reconciler + deterministic roundKey.
decisions: recommended oracle staleness model = shallow history-window cell (B), over latest-only
  (A) / inline-proof (C) / fit-existing-lean-oracle. Asked the user to lock it (contract-rippling).
gotchas: user did NOT pick the oracle model — they want to consult ANOTHER AGENT for an independent
  proposal first, then compare/contrast. Gave them a portable summary + two compare prompts
  (ask the other agent the same oracle-staleness question; ask what it partitions the oracle by).
  Work is PAUSED pending the other agent's proposal. Nothing implemented; current code untouched.
next: when user returns with the other agent's architecture, do a side-by-side — agreements, real
  disagreements, which to adopt. THEN converge on oracle staleness model and turn it into a concrete
  module/process layout reconciled with existing packages/watcher.

## CONVERGED ARCHITECTURE DECISION (cross-agent compare + 2 user picks)
status: done
intent: compare my proposal vs a second agent's, resolve the genuine divergences, lock a design.
did: strong convergence between both agents — lane=feed×duration, recompute-from-time, chain-as-truth,
  oracle-by-FEED, fail-safe VOID, single-binary role-split. Both INDEPENDENTLY chose the oracle
  history-window staleness model → LOCKED (was the open question). Resolved the real divergences and
  the user picked the two contested calls. FINAL AGREED ARCHITECTURE:
  - FOUR partition axes: oracle→FEED, lifecycle→LANE, execution→WALLET, indexer→NETWORK; API stateless.
  - Scheduler: LEVEL-TRIGGERED reconciler, recompute desired state from lane config + chain each sweep,
    NO durable jobs table for lifecycle; wake interval COMPUTED from nearest grid boundary (not fixed
    poll). tx_log stays as the only persisted work-state (broadcast→commit in-flight guard). [user pick]
  - Partition grain now: each lane an independent planner unit in code; wallets grouped BY CADENCE at
    first (one wallet = all assets at a duration), fanning into a PER-WALLET SERIALIZED executor queue;
    re-shard wallets later without touching lane logic. [user pick]
  - Oracle: one publisher per feed cell; rolling HISTORY-WINDOW cell; ADAPTIVE/boundary-aware publish
    cadence; store Pyth `confidence` + gate on it; feed-health CIRCUIT-BREAKER gates CREATE (not just
    resolve). publish_time = authoritative clock for price phases, chain `now` only gates CLOSE grace.
    Contract must verify EARLIEST qualifying tick (∈[boundary, boundary+grace], none earlier in window).
  - Data: chain = keeper truth; `rounds` = derived log (NOT truth) split from `pools` chain-projection;
    DB-backed config registry for runtime enable/disable; SQLite now, Postgres only when API needs HA.
decisions: ADOPTED from agent-2 over my v1: keeper-by-lane / executor-by-wallet split; adaptive+
  boundary-aware oracle publishing; confidence gating; feed circuit-breaker on CREATE; config registry;
  rounds/pools split; network isolation. REJECTED from agent-2: durable jobs queue for lifecycle (it
  contradicts recompute-from-chain and can drift) — recompute instead. KEPT from my v1: publish_time-as-
  clock, earliest-tick verification subtlety, fail-safe framing.
gotchas: this is a TARGET design; existing packages/watcher partially diverges (keeps keeper≈wallet
  coupling, no config registry / adaptive oracle / confidence / circuit-breaker / rounds split yet,
  oracle is StubOracleSource). NOT YET reconciled to code or implemented — design only.
next: turn the converged design into a concrete module/process layout reconciled with the existing
  packages/watcher: (a) decouple lane-planner from wallet-executor (per-wallet serialized queue);
  (b) build the real OracleSource = ingester(Pyth Hermes)→store(+confidence)→publisher(history-window,
  boundary-aware)→read-adapter(earliest-tick), replacing StubOracleSource (phase 7); (c) feed-health
  circuit-breaker into planCreates; (d) config registry table + rounds/pools table split. Confirm the
  lean-oracle on-chain format actually supports a history window before building the publisher.

## Lifecycle correction + deposit-concurrency decision (2026-06-25)
status: done
intent: user refined the model — fold in deposits, the challenge window, and FINALIZE (which my
  earlier walkthrough dropped), and decide the deposit-concurrency approach (intent/solver vs direct).
did: corrected lifecycle = CREATE → deposits → LOCK → SETTLE → CHALLENGE WINDOW → FINALIZE → redeem →
  grace → CALL-IN. Key point: SETTLE only CLAIMS the outcome; redeem is gated on FINALIZE, not SETTLE;
  the challenge window between them is the dispute period (CORRECT-settle), verified against the
  on-chain oracle history — so the history-window oracle + challenge window are one safety mechanism.
  Decided deposit concurrency: PoolCell is a hot cell (~1 deposit/block/pool contention). GO DIRECT
  NOW (Tx-Builder API retries on cell-conflict); contract deposit is already invariant-based so an
  intent-cell + solver batcher is a future DROP-IN (no contract change), single-writer of the PoolCell
  during OPEN, handing off to keeper at LOCK, with direct deposit as fallback. Trigger: ~1/block.
  Confirmed feeds are extensible by design (per-feed oracle partition). Produced a corrected
  end-to-end single-round walkthrough (BTC-15m:21:00) with keeper-vs-user tx attribution.
decisions: direct deposits now, solver later (user agreed it's overkill for now); recorded into
  backend-architecture-decision memory.
gotchas: challenge-window length + finalize/void timing are lane-config (illustrative ~2min for 15m);
  close_grace(d)=clamp(d*8,1h,7d). REDEEM also touches the treasury cell (serializes) but low-volume →
  plain retry fine. Still design-only; nothing implemented.
next: unchanged — turn converged design into concrete module/process layout reconciled with existing
  packages/watcher; first buildable unit is the real OracleSource (ingester→store→publisher→adapter).
  Before building publisher, confirm lean-oracle on-chain format supports a history window.

## Grounded OracleSource in the repo's decided design + heartbeat decision (2026-06-25)
status: done
intent: moving to build OracleSource — first ground the abstract oracle design against the ACTUAL
  contract + specs before writing code.
did: read oracle_read.rs, pool_type/main.rs oracle logic, watcher OracleSource/StubOracleSource,
  game-sdk oracle adapter + OracleTick, docs/oracle-lane-spec.md, docs/timing-spec.md. FOUND the repo
  already has a fully-decided, contract-IMPLEMENTED oracle design that SUPERSEDES 3 of our session's
  abstract decisions: (1) PERMISSIONLESS — winners race to advance+resolve; watcher = liveness backstop,
  NOT authority; safety = price band (boundary,void_time) + oracle_commit, not keeper honesty.
  (2) NO on-chain history window — Lean Oracle cell is latest-only; earliest-tick via provisional
  resolve + permissionless CORRECT (strictly-earlier-wins) + FINALIZE latch; history lives in Hermes +
  mint-fresh-authenticated-cell-at-historical-tick. (3) per-LANE cells (churn/monotonicity), though
  per-feed still correct (tunable knob). Confidence NOT in contract. Two graces: contest
  grace=clamp(d/10,60,600) (90s/15m), teardown close_grace=clamp(d*8,1h,7d) (2h/15m). Corrected the
  memory.
decisions: user picks for v1 OracleSource — PER-FEED cell (simpler); advancement = HEARTBEAT PRIMARY +
  ON-DEMAND FALLBACK. Heartbeat advances each feed cell every smallest cadence on the grid (cadences
  must be integer multiples of heartbeat → every boundary is a heartbeat mark → activate/resolve are
  pure reads). On-demand (same Hermes+pullUpdate, reconciler-triggered) covers FINALIZE/VOID ticks
  ≥void_time (heartbeat grid hasn't landed them yet) + stale-cell fallback (dead heartbeat else VOIDs
  all rounds). Heartbeat does NOT avoid Hermes+pullUpdate — just changes the trigger.
gotchas: pure-heartbeat delays finalize up to one heartbeat interval (~3.5min for 15m) since void_time
  (B+90s) falls before the next grid tick (B+5m) — hence the on-demand path is needed, not optional.
  CellDep churn race exists with per-feed cell but is managed under a single watcher + prompt
  (within-grace) transitions; a too-late re-dep is out-of-band → VOID (fail-safe). Still nothing
  implemented yet — this was grounding + decision.
next: BUILD v1 OracleSource components: (1) HermesClient.getFirstUpdateAtOrAfter(feedId,t); (2)
  LaneOracleCell wrapper over lean-oracle-sdk (getState/pullUpdate/rebalanceFuel); (3) OracleAdvancer
  (heartbeat loop on smallest-cadence grid + advanceTo on-demand); (4) LiveOracleSource implementing
  the existing getTickAtOrAfter, orchestrating the above, tolerant of being raced past. Confirm
  lean-oracle-sdk is available as a dep + its pullUpdate/rebalanceFuel/getOracleCellState API shape
  before wiring. Then wire into service (replace StubOracleSource) behind config.

## Studied lean-oracle-sdk (local sibling repo) (2026-06-25)
status: done
intent: user said the lean-oracle SDK is a LOCAL sibling repo — go read it and understand it before
  building OracleSource (will be npm-installed when we build).
did: located it at /home/destiny/ckb/lean_oracle, package `lean-oracle-sdk` (packages/sdk, ESM,
  @ckb-ccc/core dep). Read index/client/hermes/workflows/network/cells/deployment/pipeline/fuel +
  README. FINDING: the SDK already implements ~everything OracleSource needed — Hermes fetch
  (latest + boundary-anchored at/{t}), oracle cell read (getOracleCellState, full decoded OracleData
  incl. conf/expo), update-draft (draftOracleUpdateTx), fuel/fee funding (rebalanceFuel), deploy
  (initiateOracleDeployTx) + permissionless always-success lock preset, feed discovery
  (findPythFeedIdBySymbol). CONFIRMED LeanOracleClient.getOracleCellState structurally satisfies
  game-sdk's OracleStateReader → game-sdk resolveOracleTick maps it straight to an OracleTick (read
  path already written). Recorded API surface + the oracle_commit coupling in a new reference memory.
decisions: OracleSource build collapses to a THIN layer — LiveOracleSource (orchestrate
  getOracleCellState→OracleTick + advance-on-miss) + OracleAdvancer (heartbeat loop + on-demand;
  advance = draftOracleUpdateTx→rebalanceFuel→keeper send) + config/wiring; do NOT build HermesClient
  or cell codecs (use SDK). Replace StubOracleSource in service.
gotchas: (1) `oracle_commit` coupling — pool CREATE must compute commit from the SAME lean-oracle
  deployment identity (oracle code hash + guardianSetTypeHash + pyth emitter chain/addr) the advancer
  feeds, or find_oracle rejects the cell → must live in LaneConfig.oracleIdentity, shared by CREATE +
  source. (2) keeper wallet funds oracle-update txs via rebalanceFuel (needs capacity headroom). (3)
  NO devnet preset — hand-build LeanOracleNetworkConfig (hermes+rpc+devnet deployment artifacts from
  lean_oracle's own deployment/ toolbox). (4) price is raw mantissa×expo but up/down compares same-
  feed start vs settle so exponent-consistent; conf available off-chain though contract ignores it.
  Still nothing implemented — this was study only.
next: BUILD remaining thin layer when user says go: (a) devnet LeanOracleNetworkConfig + feed config
  (symbol→feedId); (b) ensure a lean-oracle cell is DEPLOYED per feed (initiateOracleDeployTx) and the
  pool oracle_commit/LaneConfig.oracleIdentity matches it; (c) OracleAdvancer (heartbeat + on-demand);
  (d) LiveOracleSource impl + replace StubOracleSource behind config; (e) tests. Decide testnet vs
  devnet target for first live wiring.

## Deploy a fresh lean-oracle on devnet to test (2026-06-25) — IN PROGRESS
status: in_progress
intent: stand up ONE shared offckb devnet at :8114 (pool+oracle co-located) and deploy a fresh
  BTC/USD lean-oracle via lean_oracle/deployment toolbox, then verify it advances with a live Hermes
  VAA. User approved plan + use offckb pre-funded dev key. Prior May deploy is stale (nothing running;
  was on :28114). Toolbox + oracle binaries already built.
did: (starting) — see below as steps land.
next: start devnet → configure lean_oracle .env to :8114 + secp overrides (reuse pool repo's) + dev key
  → validate:config → 6-step deploy (dry then broadcast) → verify advance → capture identity for
  LaneConfig.oracleIdentity + pool oracle_commit.

## Devnet oracle deploy + advance VERIFIED (2026-06-25)
status: done
did: stood up ONE offckb devnet at :8114 (node bin 0.205.0 `ckb run -C ~/.local/share/offckb-nodejs/devnet`
  + `ckb miner`, both background; `offckb node` is broken — "Not a CKB directory" + it only runs a proxy
  on :28114, which is why lean_oracle .env pointed there. 8114=direct ckb RPC, 28114=offckb ws/proxy =
  SAME chain). Resumed prior chain (tip ~97k) — May deploy's CODE cells (guardian-set-type, oracle-type
  v2, owned-type-bind-lock) + guardian-set STATE cell all still LIVE; only the original oracle cell was
  spent. So skipped steps 1-5, ran ONLY `deploy:oracle` (reusing live code+guardian set) → fresh BTC/USD
  oracle cell 0x9138b9c6…:0 (298 CKB, 152-byte OracleData). Then wrote scratchpad/verify-advance.mjs
  (reuses lean_oracle SDK integration helpers loadDevnetDeploymentFixture + createDevnetSignerFixture +
  rebalanceFuel) and advanced via a live Hermes VAA: committed, publish_time May→2026-06-25T09:39:08Z,
  price $61,654.585. On-chain Wormhole verification against the guardian set PASSED.
config: lean_oracle/deployment/.env now points DEVNET_CKB_RPC_URL=http://127.0.0.1:8114, deployer=offckb
  dev #0 (0x6109…8cd6), secp overrides copied from pool repo (match this devnet's genesis — verified the
  dep cell 0x4d804f… is live), feed=BTC/USD, DRY_RUN=true (broadcasts used inline DRY_RUN=false BROADCAST=true).
ORACLE IDENTITY (for pool oracle_commit + LaneConfig.oracleIdentity):
  oracleTypeCodeHash = 0x10c9bcc3af00fc3728cb95d5e14ec882716af5f531a010852526ce784f6958ec (v2, data2)
  guardianSetTypeHash = 0xf1e47cb23af36f2b8662f510e0bbb2cbf5a7669886cb96574a95a67751693b98
  emitterChain = 26 ; emitterAddress = 0xe101faedac5851e32b9b23b5f9411a8c2bac4aae3ed4dd7b811dd1a72ea4aa71
  feedId(BTC/USD) = 0xe62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43
gotchas: (1) DEVNET FEE-RATE QUIRK — get_fee_rate_statistics returns null on a low-traffic devnet and
  CCC's getFeeRate throws; the watcher's OracleAdvancer MUST override client.getFeeRate with a fallback
  (used 5000 shannons/KW; 1000 was just under the node min-fee → LowFeeRate). (2) discovery returns the
  highest-publish_time live cell, so the dormant pt-0 deploy cell (0x9138b9c6) is harmless; the advanced
  head is 0x2b0ed256…. (3) devnet left RUNNING: node bg=bhii9fvtj, miner bg=bu6o0cvb8.
next: BUILD the watcher OracleSource for real — devnet LeanOracleNetworkConfig (mirror
  loadDevnetDeploymentFixture from artifacts), LiveOracleSource (getOracleCellState→OracleTick + advance-
  on-miss), OracleAdvancer (heartbeat + on-demand; with the getFeeRate devnet fallback), replace
  StubOracleSource. Then thread oracleIdentity into pool CREATE/LaneConfig so oracle_commit matches.

## Built the real OracleSource in the watcher (2026-06-25)
status: done
did: replaced the stub-only oracle path with a real, tested implementation.
  - `src/oracle/liveSource.ts` — `LiveOracleSource` (pure orchestration over injected
    `readCell`/`advance` effects): read the feed's live cell; if behind the boundary floor,
    advance once and re-read; map to OracleTick or null. SDK-free, unit-tested.
  - `src/oracle/leanSource.ts` — `createLeanOracleSource` factory wiring the effects to
    lean-oracle-sdk: readCell=getOracleCellState; advance=fetchHermesPriceUpdatesAtPublishTime →
    draftOracleUpdateTx → rebalanceFuel(feeRateShannonsPerKbOverride) → signer.sendTransaction →
    waitTransaction. Only watcher module importing lean-oracle-sdk.
  - `src/oracle/leanNetwork.ts` — loadLeanNetwork(jsonPath) + oracleIdentityOf(network) (derives
    OracleIdentity, recomputing guardianSetTypeHash from the guardian-set type script so pool
    oracle_commit matches find_oracle).
  - `src/presets/lanes.ts` — defaultBtcLanes now takes an optional OracleIdentity.
  - `src/bin/server.ts` — WATCHER_ORACLE=live builds the lean source from WATCHER_ORACLE_CONFIG
    (a LeanOracleNetworkConfig JSON) + derives the lane identity from it; else the stub.
  - dep: added "lean-oracle-sdk": "file:../../../lean_oracle/packages/sdk" (symlinked).
  - test: tests/oracle.fixture.mjs (4 cases). Full suite 18/18; typecheck + build clean.
decisions: pass the watcher's CCC client/signer into lean-oracle-sdk despite it bundling its OWN
  @ckb-ccc/core@1.12.2 (watcher has 1.12.5). PROVEN runtime-compatible (verify-advance.mjs did exactly
  this mix and committed). The mismatch is TYPE-ONLY → contained `as unknown as Lean*` casts at the
  bin boundary + lean-derived types (LeanCccClient/LeanScriptLike/LeanTx) in the factory. No dedupe
  attempted (cross-repo file: links each keep their own ccc).
gotchas: NOT yet run live as a service — needs (a) a generated LeanOracleNetworkConfig JSON for devnet
  (mirror lean_oracle's loadDevnetDeploymentFixture), (b) pools deployed with the matching oracle_commit,
  (c) the devnet running (node+miner were KILLED at end of session — restart with
  `~/.local/share/offckb-nodejs/bins/0.205.0/ckb run -C ~/.local/share/offckb-nodejs/devnet` + miner).
  Heartbeat advancer (latency optimization) NOT built — on-demand advance alone is correct (finalize/VOID
  fetch a >=void_time tick on demand); heartbeat just reduces latency. Devnet getFeeRate fallback wired
  via WATCHER_ORACLE_FEE_RATE (default 5000).
next: (1) generate devnet LeanOracleNetworkConfig JSON; (2) deploy pool contracts + create a pool with
  oracleIdentityOf(network) so oracle_commit matches; (3) run watcher role=all WATCHER_ORACLE=live against
  devnet and watch a full CREATE→activate→resolve→finalize lifecycle advance live; (4) add the heartbeat
  OracleAdvancer. Optionally export a small script to emit the network JSON from lean_oracle artifacts.

## Proved the real OracleSource LIVE on devnet (2026-06-25)
status: done
did: restarted devnet (node bg=bvi8a0w4a, miner bg=bmvtl1zd4 — RUNNING). Generated the watcher's
  devnet oracle config at packages/watcher/config/lean-oracle.devnet.json (via lean_oracle's own
  loadDevnetDeploymentFixture helper → emitted fixture.network, bigint→number). Then exercised the
  WATCHER'S OWN code path live (loadLeanNetwork → oracleIdentityOf → createLeanOracleSource →
  LiveOracleSource.getTickAtOrAfter): for a past boundary it advanced the oracle cell on-demand (tx
  0xaaf39879…) and returned the EARLIEST tick at/after the boundary (publishTimeUnix == boundary
  exactly, price ~$59,401, valid cellDep). oracleIdentityOf derived the correct identity
  (0x10c9bcc3 / 0xf1e47cb2 / 26 / 0xe101…) matching the deployed oracle. Temp harness removed.
findings: (1) NO clock skew — env/chain/Hermes all within ~6s of real 2026-06-25T17:49Z. The initial
  404 was only because I requested a tick at EXACTLY now; Hermes' latest lags real-now by a few
  seconds, so "now" was momentarily in its future. Real lifecycle resolves a PAST close_time → fine.
  Use minPub slightly in the past. (2) POOL CONTRACTS partially gone on this resumed chain: poolType
  v2 code cell (0x39404f0) is LIVE but shareCode/treasuryLock/adminLock code cells are SPENT/unknown.
  So a full keeper CREATE→…→finalize run is BLOCKED until the pool contracts are redeployed + the SDK
  devnet preset (packages/game-sdk/src/presets/devnet.ts) refreshed with new hashes/outpoints.
decisions: the OracleSource milestone (build + prove live) is COMPLETE. The full pool-lifecycle live
  run is a separate ops chain (pool-deployment toolbox redeploy + preset refresh + real-time waits per
  cadence) — did NOT auto-start it; it crosses into the pool-deployment subsystem.
gotchas: config/lean-oracle.devnet.json holds devnet-specific outpoints — regenerate after any oracle
  redeploy. To run the watcher live now: WATCHER_ORACLE=live WATCHER_ORACLE_CONFIG=./config/lean-oracle.devnet.json
  WATCHER_CREATOR_PRIVATE_KEY=0x6109… + the devnet secp env — but it will CREATE pools that fail to
  build until the 3 missing pool contracts are redeployed.
next: to land the full live lifecycle — (1) redeploy pool contracts via deployment/ toolbox on this
  devnet; (2) refresh game-sdk devnet preset hashes; (3) run watcher role=all WATCHER_ORACLE=live and
  watch CREATE→activate→resolve→finalize advance (5m lane = minutes per phase); (4) build the heartbeat
  advancer to cut finalize latency. Devnet + oracle are live and ready.

## Built the oracle heartbeat advancer (2026-06-25)
status: done
did: added `src/oracle/heartbeat.ts` — `OracleHeartbeat`: keeps each feed's oracle cell pre-positioned
  at the first tick of the current smallest-cadence grid bucket. Self-aligning setTimeout (fires just
  after each grid mark + postMarkDelayMs=2s so Hermes has the first-tick-≥-mark, avoiding the exact-now
  404), anchors via source.getTickAtOrAfter(feed, mark) which uses /v2/updates/price/{mark} and no-ops
  when the cell is already fresh → ~one advance per grid bucket. Re-entrancy guard (no overlapping
  ticks), per-feed error isolation, warm-on-start. Wired into service.ts (ServiceDeps.oracleHeartbeat;
  starts when runsKeeper && flag; gridSecs = min lane cadence; feeds = distinct lane feeds; start/stop
  in lifecycle). bin/server.ts passes oracleHeartbeat: oracle!==undefined (on iff WATCHER_ORACLE=live).
  Exported OracleHeartbeat from index. 3 unit tests (anchoring to floored mark, re-entrancy, error
  isolation). typecheck + build clean; full suite 25/25 (was 18 + 3 heartbeat... oracle.fixture now 7).
decisions: heartbeat REUSES getTickAtOrAfter rather than its own advance — so the on-demand fallback,
  earliest-tick /{publish_time} anchoring, and null-on-fail all come for free and it no-ops when fresh.
  Per the Hermes-endpoint lesson: heartbeat anchors to the grid MARK (not latest), landing the canonical
  first-post-boundary tick. On-demand advance stays the fallback (esp. FINALIZE's ≥void_time tick).
gotchas: heartbeat advances move the keeper wallet's capacity each grid bucket (funds oracle updates) —
  bounded to ~1 advance/bucket. NOT separately run live (getTickAtOrAfter is already proven live; the
  heartbeat is thin tested orchestration over it). Devnet (node bvi8a0w4a + miner bmvtl1zd4) still up.
next: the remaining milestone is the full keeper lifecycle live run — still blocked on redeploying the
  3 missing pool contracts (share_xudt, treasury_lock, pool_admin_lock) + refreshing game-sdk devnet
  preset, then run watcher role=all WATCHER_ORACLE=live and watch CREATE→activate→resolve→finalize.

## Contract band fix: inclusive lower bound + full pool redeploy + live run (2026-06-25)
status: in_progress
intent: live keeper run surfaced an activate failure — "oracle tick (publish_time == start_time) is
  not in an activatable band". User correctly diagnosed it as a CONTRACT bug: the band lower bound was
  STRICT (start < pub) but should be INCLUSIVE (start <= pub < close), since a tick exactly at the
  boundary second is the canonical boundary price and already proves real time reached it.
did: CONTRACT (pool_type/main.rs) — set_start_tick `> start` → `>= start`; set_settle_tick
  `> close` → `>= close`; validate_activate one-sided VOID past_start `> start` → `>= start`; updated
  doc comments to [start,upper)/[close,upper). SDK (keeperTransitions.ts) — mirrored all four
  (activate/correct-start/resolve/correct-settle) to `>=` lower bound + reworded errors. TESTS — flipped
  Rust activate_tick_at_start_fails→succeeds, resolve_tick_at_close_fails→succeeds; repointed
  correct_start/settle "before bound" tests to START-1/CLOSE-1; SDK txKeeper fixtures: tick-at-start→
  LOCKED, tick-at-close→SETTLED (+ before-bound throws), fixed 2 error-message regexes. timing-spec.md
  band notation updated (≤ lower, < upper) + rationale. 84 Rust tests pass; game-sdk suites pass.
  REDEPLOY: all 4 pool contracts redeployed+promoted fresh on devnet earlier (cell contention forced
  one-broadcast-per-call; RBF/leastFee errors were transient mempool conflicts that self-clear). After
  the band fix, pool_type rebuilt + redeployed again → now v5 (code 0xa6d78c37…, tx 0x1d62f2bd…).
  Refreshed game-sdk devnet preset: poolType→v5, shareXudt→0x2101f97c (tx 0x0c2b4a99), treasuryLock/
  poolAdminLock code unchanged but new outpoints (0x86101c1b / 0x929e0ddc), poolTypeVersions {1,5}.
  Rebuilt sdk+watcher. Restarted watcher live (BTC-5m, WATCHER_ORACLE=live) — CREATE succeeded
  (0x5893feb0), pool OPEN; awaiting activate→resolve→finalize.
gotchas: KEEPER FEE QUIRK — the keeper's complete() also hit the devnet get_fee_rate_statistics-null
  error on the FIRST create (same as oracle); it self-resolved once recent blocks had txs (fee stats
  populated). The watcher's keeper path has NO explicit fee fallback (only the oracle advancer + player
  txBuilder do) — a real gap to fix (give keeper complete() a devnet fee override) so a cold devnet
  doesn't stall the first transitions. Heartbeat had one transient Hermes "fetch failed" (retries next
  tick). Devnet bg: node bvi8a0w4a, miner bmvtl1zd4. Live-run bg: bjz9qodil.
next: confirm activate→resolve→finalize land live (waiter bey6t2k5g watches for finalize/failure). Then:
  fix the keeper-complete devnet fee fallback; consider redeem (user tx). Update memory after confirming.

## Live run results: band fix VERIFIED; full lifecycle blocked by keeper ops bugs (2026-06-25)
status: done (for this session's scope)
did: ran the watcher live (BTC-5m, WATCHER_ORACLE=live) against the redeployed devnet. PROVEN:
  - CREATE works (multiple pools minted + indexed + served by API).
  - The contract BAND FIX works: activate fires at the boundary tick. Empty pools (no players)
    correctly transition OPEN→VOID at activate (one-sided/empty), confirming the activate path runs.
  - Deposits work: seeded a pool two-sided (300 UP / 200 DOWN via PlayerClient + dev#1, feeRate:5000)
    → pool upTotal/downTotal set, status OPEN.
  FULL activate→LOCKED→resolve→finalize NOT achieved live — blocked by KEEPER OPERATIONAL bugs (NOT
  contract/oracle, which are proven):
  1. SINGLE-WALLET CELL CONTENTION: the keeper fires a CREATE every poll (15s) + activate + heartbeat
     advances all on ONE wallet (dev#0). complete() built the activate against an optimistically-tracked
     output of an UNCONFIRMED/failed create tx → node `TransactionFailedToResolve: Unknown(OutPoint(
     0xf8bb14db…))`. This is exactly what the agreed design's PER-WALLET SERIALIZED EXECUTOR QUEUE +
     "wait for commit / don't spend unconfirmed" is meant to prevent; the current keeper is naive here.
  2. on-demand advance returned null ("skip activate: no oracle tick >= start") when start_time was
     momentarily at/ahead of Hermes' latest (the "asking for ~now" 404 edge) — transient, retried.
  3. watcher API `?status=open` filter returns EMPTY while the unfiltered list shows the pool open — a
     real API filter bug.
  4. keeper complete() has NO devnet feeRate fallback (only oracle advancer + player txBuilder do); the
     first CREATE hit get_fee_rate_statistics-null and self-resolved once blocks had tx history.
  Stopped the watcher (was looping failed activates). Devnet (node bvi8a0w4a, miner bmvtl1zd4) still up.
gotchas: the keeper ops bugs are the real remaining work to see a clean live resolve→finalize. The
  contract (84 Rust tests) + SDK + OracleSource + heartbeat are all proven correct; this is operational
  hardening of the keeper's tx/cell lifecycle on a single wallet.
next (keeper hardening, in priority order):
  1. Keeper tx/cell management: serialize wallet ops; WAIT for a CREATE to commit before building
     dependent txs; don't build against unconfirmed outputs; throttle CREATE (don't re-mint every tick).
     This is the per-wallet serialized executor queue from backend-architecture-decision.
  2. keeper complete() devnet feeRate fallback (pass { feeRate } like the lifecycle test does).
  3. fix watcher API `?status=open` filter.
  4. then re-run live and confirm activate→LOCKED→resolve→finalize on a two-sided pool.

## Keeper hardening: serialize wallet ops + fee fallback + API filter fix (2026-06-25)
status: in_progress (live re-test running)
intent: fix the keeper operational bugs that blocked the live lifecycle (cell contention →
  "Unknown OutPoint", missing devnet fee fallback, broken ?status=open filter).
did:
  - NEW src/mutex.ts — minimal async serial queue (Mutex.run) + noopMutex. Serializes every
    wallet-spending op (keeper transitions + oracle advances) since they share one wallet + CCC
    client; concurrent cell selection was picking the same cells → tx referenced another's
    unconfirmed output. Wrapped the WHOLE high-level op (a keeper action incl. its internal oracle
    advance, and each heartbeat advance) in one shared mutex — so no advance interleaves between a
    transition's tick-read and its send (keeps the oracle cell-dep live). Low-level advance does NOT
    self-lock (avoids re-entrancy/deadlock). Exported Mutex/noopMutex; mutex.fixture.mjs (3 tests).
  - executor.ts: ExecContext.feeRate; KeeperLike.complete gains options; pass { feeRate } to
    complete() — fixes the devnet get_fee_rate_statistics-null crash on the keeper path.
  - service.ts: one walletMutex; keeperTick wraps `execute(action)` in walletMutex.run; OracleHeartbeat
    gets lock: walletMutex; execCtx.feeRate = config.feeRate ?? DEFAULT_FEE_RATE (1000).
  - heartbeat.ts: optional `lock`, wraps each getTickAtOrAfter in it.
  - api/server.ts: ?status= now accepts the status NAME (STATUS_CODE reverse map + parseStatusQuery),
    not just a numeric code — fixes ?status=open returning empty.
  Full watcher suite green (added mutex fixture); typecheck + build clean.
  LIVE RE-TEST (watcher bg b0cf73okk): CREATE succeeds with NO fee crash; ?status=open returns the
  pool (filter fixed); heartbeat + keeper serialized. Seeded pool 0x1dff77a2 two-sided (300 UP/200
  DOWN via dev#1, separate wallet from keeper dev#0) — activates in ~4.3min. Waiter b1qageo9w watches
  for finalize/failure (the run that should show activate→LOCKED→resolve→finalize end-to-end).
gotchas: keeper feeRate defaults to 1000 (config.feeRate ?? DEFAULT); the oracle advancer uses its own
  WATCHER_ORACLE_FEE_RATE (5000, since small oracle txs need higher). Devnet bg: node bvi8a0w4a, miner
  bmvtl1zd4. Deposits use dev#1 (distinct wallet) so they don't contend with the keeper.
next: confirm the waiter shows finalize; capture start/settle/winner; then redeem (winner claim) is the
  last user-facing piece. Update memory after confirming the full live lifecycle.

## FULL LIVE LIFECYCLE PROVEN END-TO-END (2026-06-25)
status: done
did: after the keeper hardening (mutex serialization + feeRate + ?status filter) the activate STILL
  failed with Unknown OutPoint — root cause found: the deposit (external wallet/client) MOVES the pool
  cell, and the keeper's CCC client CACHE still held the pre-deposit outpoint (cached at create), so it
  built the activate against a spent cell. FIX: `await client.cache.clear()` at the top of keeperTick +
  indexTick (service.ts) so reads reflect external spends — essential since players always deposit from
  external wallets. typecheck + build clean.
  RE-RAN live: seeded pool 0x5c23bd35 two-sided (dev#1) → the hardened keeper carried it through the
  COMPLETE lifecycle on the live oracle:
    CREATE → activate→LOCKED (0xf5adb420) → resolve→SETTLED (0x0d74a510) → finalize→FINALIZED (0x94ac1f25)
    winner = UP. Final state confirmed: status=finalized, winner=up, finalize tx committed.
  This is the milestone: keeper drives the full CREATE→activate→resolve→finalize live, oracle advances
  via Hermes (heartbeat + on-demand), contract band fix lets activate fire on the boundary tick, all
  txs serialized on one wallet with fresh cell reads. The four keeper bugs are FIXED + verified live.
gotchas: COSMETIC indexer gap — the API serializes startPrice/settlePrice/upTotal/downTotal as null for
  the finalized pool (status + winner are correct + the on-chain data has the prices). The indexer's
  projection of those fields for terminal pools should be checked (follow-up, not blocking). On-demand
  advance still occasionally 404s when a boundary ≈ Hermes-now (transient, retries). Devnet bg: node
  bvi8a0w4a, miner bmvtl1zd4; watcher bg bhf983hg1 (still running).
next: (optional) REDEEM — the winner (UP, dev#1) claims via PlayerClient.draftRedeem to prove the
  user-facing payout path end-to-end. Fix the indexer null-field projection for terminal pools. The
  keeper/oracle/contract are now proven correct live.

## WINNER REDEEM — full money loop closed (2026-06-26)
status: done
did: dev#1 (held all 300 UP of FINALIZED winner=UP pool 0x5c23bd35) redeemed via
  PlayerClient.draftRedeem({poolId, redeemerLock}) → complete(feeRate 5000) → send (tx 0x4ab457a4,
  committed). Result: UP shares 300→0 (burned), DOWN 200 untouched (losing, reclaimable via burn),
  wallet +496.0000 CKB = EXACT parimutuel payout (300 UP stake + 196 = the 200 DOWN losing pool minus
  2% rake of 4). Rake (4 CKB) stays in the pool to sweep to the creator at CLOSE.
  FULL END-TO-END NOW PROVEN LIVE: CREATE → DEPOSIT → activate→LOCKED → resolve→SETTLED → finalize→
  FINALIZED → REDEEM (payout 496). Keeper-driven transitions on the live oracle + user deposit/redeem
  via PlayerClient, hardened keeper (mutex + cache.clear + feeRate), fixed contract (inclusive band).
next: optional polish — fix indexer null-field projection for terminal pools (startPrice/settlePrice/
  totals serialize as null); burn the losing DOWN shares to reclaim capacity; wire the watcher keeper
  feeRate fallback is done. Devnet + watcher (bg bhf983hg1) still up.

## "Indexer null-field projection" was a FALSE ALARM (2026-06-26)
status: done
did: investigated the supposed indexer bug. NO BUG — the API projects everything correctly. The
  earlier "null" was MY diagnostic querying top-level fields (startPrice/settlePrice/upTotal/downTotal)
  that aren't the API's shape. serializePool (api/server.ts) emits prices under `prices.{start,settle,
  usedPt}` and totals under `odds.{up,down}.pool` (+ odds.total, rakeIfUpWins/DownWins). Confirmed live
  on the finalized pool: prices.start=5966718500000 ($59,667.19), prices.settle=5975799250000
  ($59,757.99 → UP wins), odds.up.pool=300 CKB, odds.down.pool=200 CKB, rakeIfUpWins=4 CKB. Correct.
  Frontend should read prices.* and odds.*.pool (the web mock already matches this contract).
next: no fix needed. Remaining optional polish: burn losing DOWN shares to reclaim capacity; transient
  Hermes 404 self-retries. The watcher (oracle + keeper) + SDK + contract are proven correct live,
  full money loop closed (CREATE→deposit→activate→resolve→finalize→redeem, payout 496).

## Positions: removed storage → read on-demand from chain (2026-06-26)
status: done
intent: user — positions shouldn't be stored; fetch from chain when needed. (Per AskUserQuestion:
  the WATCHER API serves them on-demand, keeping the frontend thin + honoring [[frontend-no-sdk-rule]]
  — not the browser querying chain directly, which would duplicate share-cell decode in-browser.)
did: removed the `positions` table (db/schema.ts) + all position indexing. indexer.ts no longer lists
  share cells (dropped shareCellsToPositions + listShareCells; indexOnce returns {pools} only) — kills
  the per-tick O(all-holders) share scan that churned on every deposit/redeem/burn. db.ts dropped
  PositionRow + replacePositionsForPool + positionsForHolder + the position statements. api/server.ts:
  new `Position`/`PositionsReader` types + `positions?` on ApiDeps; `/positions` and
  `/pools/:id/positions` now take `?address=` (holder CKB address) and serve on-demand (501 if no
  reader, 400 if no address). service.ts implements the reader: resolve address→lock via
  ccc.Address.fromString, then player.getShareBalances per non-CLOSED pool (the SDK's lock-scoped
  query). index.ts exports updated. Tests updated (api.fixture uses a stub reader + ?address=;
  indexer.fixture dropped the position tests). typecheck + build clean; full suite 65 tests pass.
  VERIFIED LIVE: GET /positions?address=<dev#1> → [{pool 0x5c23bd35, side down, amount 200 CKB}] —
  the unredeemed losing shares, read fresh from chain (UP correctly absent post-redeem); /positions
  without address → 400. Indexer log now "indexed 14 pools" (no positions).
gotchas: API contract CHANGED — positions are now keyed by `?address=` (CKB address), not the old
  `?lock=<lockHash>` (a lock hash can't be reversed to a script for the chain query). Frontend + web
  mock must send the wallet address. Per-request cost is O(non-closed pools) getShareBalances calls
  (fine for v1; a single cross-pool lock-scoped query would be the optimization but the SDK doesn't
  expose one). Devnet bg: node bvi8a0w4a, miner bmvtl1zd4; watcher bg bo99fmwu4.
next: update the web frontend/mock to call /positions?address= (was ?lock=). Optional: a cross-pool
  holder share query in the SDK to make /positions one findCells instead of O(pools).

## Decisions: per-feed oracle locked, scheduler change planned, corrections permissionless (2026-06-26)
status: done (decisions; scheduler change DEFERRED)
did: user locked 3 things.
  1. ORACLE CELLS STAY PER-FEED (not per-lane). One cell per feed, shared by all its lanes, advanced at
     the smallest cadence. The CellDep-churn worry is moot here because single-writer keeper + mutex
     serialization + cache.clear (fresh reads) already make it work live. Cost = O(feeds×cadence).
  2. SCHEDULER CHANGE (planned, deferred): replace the fixed setInterval(keeperTick, pollSecs) with a
     self-scheduling setTimeout that sleeps until the NEXT due moment — min over lanes of
     nextBoundary(now,duration) + per-pool void_time(finalize)/close+grace(close), + small post-boundary
     delay, capped at a max (~60s) as a retry/safety heartbeat. Same pattern the heartbeat already uses.
     Buys work∝events + lower latency at scale. NOT correctness (heartbeat already pins the canonical
     first-post-boundary tick; band accepts it even on a late resolve). LOW priority at 15 lanes; do it
     when lane count grows. Contained to service.ts + a pure nextDueTime() helper next to plan().
  3. CORRECT-start/settle are PERMISSIONLESS — the keeper does NOT run them. Contract + SDK already
     implement them; anyone (a winner protecting payout) submits the correction, verified on-chain
     (strictly-earlier in-band tick → first-post-boundary), latched by FINALIZE. Fits keeper=backstop.
     Rarely needed in practice since the heartbeat lands the canonical tick.
next: implement the boundary-wake scheduler only when lane count/latency warrants. Frontend still needs
  the /positions?address= update.

## Worker-split full implementation — STARTED (2026-06-26)
status: in_progress
intent: implement the agreed worker topology + supporting changes.
plan (in order): (1) ReadOnlyOracleSource — keeper reads, never advances (skip+retry if stale).
  (2) Oracle worker role=oracle — SOLE writer of all feed cells; advances at union of grid boundaries
  ∪ (boundaries+grace) [covers activate/resolve AND finalize/VOID], all times computable from lane
  config (no pool-read, no IPC), batches coincident feeds via one Hermes multi-feed fetch; own wallet.
  (3) Role wiring oracle|keeper|indexer|all; keeper uses read-only source; all = keeper(reader)+oracle
  worker+indexer. (4) Indexer indexes UNION of keeper creator locks (operatorLockHashes / listManagedPools).
  (5) Closed-detection: disappearance reconcile (mark CLOSED, guarded vs transient absence). (6) index
  cadence default → 10s. Decisions locked earlier: oracle per-feed; corrections permissionless (not
  keeper); boundary-wake scheduler deferred.
next: build (1)+(2)+(3) first (the core split) with tests, then (4)(5)(6).

## Worker split IMPLEMENTED (core) + tested + live-starting (2026-06-26)
status: in_progress (live verification running)
did: built increments 1-4 of the worker split.
  (1) ReadOnlyOracleSource (liveSource.ts) — keeper reads the cell, returns null if behind, NEVER
      advances. + tickFromCell helper (shared). createLeanReadOnlySource factory (leanSource.ts) needs
      only a client (no wallet). Refactored leanReadCell shared by both sources.
  (2) OracleWorker (oracle/worker.ts) — SOLE writer; nextDue() computes the next moment any lane needs
      a tick = min over lanes of {grid boundary, boundary+grace}, with due feeds; self-schedules
      (setTimeout) just after it; advance() drives source.getTickAtOrAfter(feed, dueTime) per due feed,
      serialized via lock, re-entrancy-guarded, error-isolated; warms cells on start. Replaces/deletes
      OracleHeartbeat (heartbeat.ts removed).
  (3) Role wiring: WatcherRole += "oracle" (config.ts). service.ts: runsOracle; ServiceDeps.oracle =
      keeper READER, ServiceDeps.oracleAdvancer = worker source; oracleWorker built for oracle/all,
      shares walletMutex; start/stop wired. bin/server.ts: builds BOTH createLeanReadOnlySource (keeper)
      + createLeanOracleSource (worker) when WATCHER_ORACLE=live; role validation includes oracle.
  (4) Indexer multi-creator-lock: indexOnce takes creatorLockHashes[] (union, de-dup by poolId);
      service passes config.operatorLockHashes ?? [creatorLock hash]; bin reads WATCHER_OPERATOR_LOCK_HASHES.
  Also: index cadence default 10s, keeper poll 15s. typecheck + build clean; full suite green (oracle
  fixture now 9: LiveOracleSource×4 + ReadOnlyOracleSource×2 + OracleWorker×3).
  LIVE (role=all, new split, bg bcn794z1k): started clean — "oracle worker on (sole writer; 1 feed)",
  keeper(reader)+indexer+oracle. Deposit poller b36emv35c seeding a pool to verify activate→LOCKED via
  keeper-reads-the-worker's-cell.
gotchas: keeper now SKIPS (returns null) if the worker hasn't advanced the cell yet → retries next poll
  (≤15s latency). In role=all keeper+worker share one wallet (mutex serializes). Increment 5
  (closed-detection) NOT done yet. Devnet bg: node bvi8a0w4a, miner bmvtl1zd4.
next: confirm activate→LOCKED live (deposit poller + watch); then increment 5 (closed-detection in
  indexer with transient-absence guard). Frontend /positions?address= still pending.

## Worker split VERIFIED LIVE + wallet-hygiene finding (2026-06-26)
status: done (split validated); 2 follow-ups noted
did: full lifecycle ran under the new split (role=all, oracle worker = sole writer, keeper = pure
  reader): pool 0x6d6becb3 CREATE→activate→LOCKED→resolve→SETTLED→finalize→FINALIZED, winner=DOWN
  (start $59,500.14 > settle $59,391.33). The keeper drove every transition by READING the cell the
  oracle worker advanced — never touching the oracle. Split coordination validated end-to-end.
FINDING (real, separate from the split): intermittent FAILED create/close with ScriptNotFound
  code_hash 0x57bddf3d (= guardian-set-type, a lean-oracle deployment cell). ROOT CAUSE: the watcher
  wallet (dev#0) is ALSO the lean-oracle deployer (lean_oracle/deployment/.env deployer = dev#0), so
  dev#0's lock OWNS the deployed guardian-set/oracle code+state cells, and the keeper's seed/fee
  selection (findCells by creatorLock) sometimes grabs a TYPED deployment cell as an input → spending
  it needs its code dep → ScriptNotFound. Txs fail harmlessly (don't commit, deployment intact) but
  it's noisy; the deposited pool still finalized (those txs didn't grab a deployment cell).
fixes (follow-ups): (1) OPERATIONAL — every role its own CLEAN wallet, NONE = the deployer key (the
  oracle advancer already dodges this via collectPlainFuelCellsByLock = plain-only). (2) ROBUSTNESS —
  keeper pickSeedInput + fee completion should SKIP typed cells (spend only plain CKB) so a shared lock
  can't pull a code/oracle/guardian cell. This reinforces the per-role-wallet deployment rule.
next: (a) docker-compose update for the new topology (add oracle role service + per-role wallets +
  WATCHER_OPERATOR_LOCK_HASHES on indexer); (b) keeper seed/fee = plain-cells-only robustness; (c)
  increment 5 closed-detection; (d) frontend /positions?address=. Devnet bg: node bvi8a0w4a, miner
  bmvtl1zd4; watcher bg bcn794z1k (running, noisy create/close failures from the wallet reuse).

## Keeper sleep-schedule (boundary-wake) + 2 wallet/scoping fixes (2026-06-26)
status: done
did: converted the keeper from a fixed setInterval poll to a SLEEP-SCHEDULE (like the oracle worker),
  per user ("we are looping instead of setting up sleeping schedules").
  - planner.ts: nextKeeperWake(now, pools, lanes) — soonest future keeper event = min over lanes of
    {boundary, boundary+grace, boundary−createLeadSecs} ∪ terminal pools' close_time+closeGrace. Pure.
  - service.ts: keeperTick now returns ms-to-next-wake; scheduleKeeper() self-schedules setTimeout to
    min(nextEvent + 5s post-boundary-delay, pollIntervalSecs cap). The cap bounds retries (e.g. a skip
    because the oracle worker hadn't advanced yet) + safety. start()=scheduleKeeper(); stop()=keeperStopped
    + clearTimeout. Log: "keeper event-driven (≤Ns)". 3 nextKeeperWake unit tests; full suite green.
  ALSO fixed 2 issues the live re-test surfaced:
  - WALLET HYGIENE: switched the live watcher wallet from dev#0 (the lean-oracle DEPLOYER, which owns
    the deployed code/guardian cells → keeper grabbed them as fuel → ScriptNotFound) to dev#2 (clean).
    ScriptNotFound GONE. Confirms the rule: operational wallets must NOT be the deployer key.
  - KEEPER SCOPING: keeperTick now lists `keeper.listPools({ creator: ownCreatorHash })` (its OWN pools
    only) instead of all pools — previously it tried to CLOSE leftover dev#0 pools (admin-gated to their
    creator) → "creatorLock hash does not match" failures. Now scoped → gone. (CLOSE is creator-only;
    the keeper manages only what it created.)
  LIVE (role=all, dev#2, bg btwx0cp71): clean startup — keeper event-driven, oracle worker sole writer,
  CREATE worked (indexed 2 pools), NO ScriptNotFound, NO FAILED close. The split's full lifecycle was
  already proven; the sleep-schedule changes only WHEN the keeper fires (transition logic identical +
  unit-tested), so not re-waiting a full finalize.
next: increment 5 (closed-detection in indexer); docker-compose update for the split topology (add
  oracle role + per-role CLEAN wallets + OPERATOR_LOCK_HASHES); keeper seed/fee = plain-cells-only
  robustness (so even a shared lock can't grab typed cells); frontend /positions?address=.

## Plain-cells-only fuel (wallet-hygiene fix) + indexer-necessity decision (2026-06-26)
status: done
did (plain cells, per user "should not exist if you use only plain cells like the lean oracle sdk"):
  filtered ALL keeper cell-selection to PLAIN cells (no type, empty data) via CCC filter
  { scriptLenRange:[0,1], outputDataLenRange:[0,1] } in 3 spots:
   1. executor.ts pickSeedInput (CREATE seed),
   2. game-sdk tx/fees.ts completeFeeAndChange (completeInputsByCapacity + completeFeeBy fuel; affects
      keeper AND players — staked-asset/share inputs are added by builders, never by capacity completion),
   3. game-sdk tx/workflows.ts initiateClose creatorInput (the creator-escape cell).
  SDK build clean, 95 SDK tests pass; watcher 70 tests pass. PROVEN LIVE on dev#0 (the lean-oracle
  DEPLOYER wallet that owns the code/oracle/guardian cells): ScriptNotFound count 0 (was 28); CREATE +
  CLOSE both succeed (closing dev#0's old terminal pools cleanly). So the wallet-hygiene rule is now
  belt-and-suspenders, NOT required — any funded wallet (even the deployer) works.
  ALSO diagnosed the recurring "exit 144": `pkill -f "dist/bin/server.js"` matched the LAUNCHING shell's
  own cmdline (it contains that string) → self-kill. Fix: run kill + launch as SEPARATE commands; the
  kill-shell's 144 is benign (it kills the watcher first).
INDEXER NECESSITY (user Q — if SDK in frontend reads chain directly, is the indexer needed?): answered
  — NOT needed for live reads (pools/odds/own-positions are chain-readable), but EFFECTIVELY REQUIRED
  for (1) HISTORY (closed pools are consumed → gone from chain → only the indexer retains them; not
  chain-queryable without block scanning), (2) node-load at scale (one shared cache vs every browser
  scanning+decoding the node), (3) aggregated/sorted markets queries. Recommendation: KEEP the indexer +
  thin frontend on the API; SDK-in-browser only reopens the no-SDK rule. (We already serve own-positions
  on-demand via the API.) Decision: keep the indexer.
next (still open): increment 5 closed-detection; docker-compose update for split topology (oracle role
  + per-role wallets + OPERATOR_LOCK_HASHES) — note: clean wallets now optional thanks to plain-cells;
  frontend /positions?address=. Live watcher bg: bx0bbm9on (dev#0, port 8092, clean). Devnet: node
  bvi8a0w4a, miner bmvtl1zd4.
