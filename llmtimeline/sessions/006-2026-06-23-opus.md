# Session 006 · 2026-06-23 · opus

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Session start — handoff ingest
status: done
intent: pick up SDK work after the session-005 contract changes; read timeline + acknowledge.
did: read state.md + session 005 in full. User scope (AskUserQuestion): sync DEPOSIT to the
  contract (drop the now-stale header dep) + add withdrawal support to the SDK. Read the current
  contract validate_deposit (main.rs:537-650) and the SDK deposit/redeem/burnShares builders +
  workflows + PlayerClient + index to plan symmetric withdrawal wiring.
files: (none — read only)
decisions: withdrawal will be a SEPARATE builder (tx/withdraw.ts) + initiateWithdraw workflow +
  PlayerClient.draftWithdraw, mirroring deposit in reverse, rather than overloading deposit with
  signed amounts — keeps each builder's invariants legible.
next: edit deposit.ts/workflows.ts to drop the header dep; then write withdraw.ts.

## DEPOSIT sync: drop the stale header dep
status: done
intent: contract's validate_deposit no longer reads the header clock (session 005), so the SDK
  must stop attaching a HeaderDep to DEPOSIT txs.
did: Removed the `headerDep` param + its doc from BuildDepositParams and stopped attaching it in
  buildDepositTx (tx now `headerDeps: []` with a comment explaining why). Removed
  `headerDep: await tipHeaderHash(...)` from initiateDeposit in workflows.ts. `tipHeaderHash` is
  retained — initiateClose still legitimately reads the header (CLOSE is the only header reader now).
files: packages/game-sdk/src/tx/deposit.ts; packages/game-sdk/src/tx/workflows.ts
decisions: kept tipHeaderHash (used by initiateClose). DEPOSIT fixtures didn't assert headerDeps,
  so no test change needed for this part.
gotchas: none.
next: add the withdrawal path.

## WITHDRAW support: builder + workflow + client + tests
status: done
intent: the redesigned invariant-based validate_deposit permits OPEN->OPEN totals to FALL, so add
  an SDK path to pull stake back out (burn shares, shrink totals + funds) — the inverse of DEPOSIT.
did: New pure builder `tx/withdraw.ts` (buildWithdrawTx): burns supplied share cells, returns any
  per-side surplus as a share-change cell, shrinks PoolCell capacity by `total` (CKB) or the
  TreasuryCell balance by `total` + pays the asset out (xUDT). Mirrors the contract share invariant
  `side_out + prev.side_total == side_in + next.side_total` and the absolute treasury check. Guards:
  non-negative amounts, positive total, status==OPEN, amount<=side total, share inputs cover the
  burn, treasury covers payout. New workflow `initiateWithdraw` in workflows.ts (auto-selects the
  withdrawer's share cells largest-first via selectShareInputs, resolves xUDT ctx, delegates).
  Wired PlayerClient.draftWithdraw and the index exports (buildWithdrawTx/BuildWithdrawParams,
  initiateWithdraw/InitiateWithdrawParams). Added 7 WITHDRAW fixtures (CKB exact/partial-change,
  reject over-total / under-covered-shares / zero-or-locked; xUDT shrink+payout, reject small
  treasury / missing pieces). typecheck clean; full suite 90/90 pass (was 83).
files: packages/game-sdk/src/tx/withdraw.ts (new);
  packages/game-sdk/src/tx/workflows.ts; packages/game-sdk/src/client/PlayerClient.ts;
  packages/game-sdk/src/index.ts; packages/game-sdk/tests/txBuilders.fixture.mjs
decisions: separate builder (not a signed-amount overload of deposit) so each builder's invariants
  stay legible. Withdrawn CKB returns via completeFeeAndChange's change (no explicit holder output),
  matching redeem's CKB payout pattern; xUDT pays an explicit asset cell. No HeaderDep (OPEN->OPEN,
  status-gated).
gotchas: withdraw is NOT yet exposed via the watcher's POST /tx/* endpoints, so the frontend
  (no-SDK rule) can't call it until a /tx/withdraw endpoint is added in packages/watcher. The
  devnet preset hash is still a snapshot behind HEAD (unchanged this session) — a live withdraw
  test needs a redeploy first.
next: (optional) add POST /tx/withdraw to the watcher mirroring /tx/deposit; refresh
  presets/devnet.ts after the next devnet redeploy.

## SDK re-layout into lean-oracle-style subpaths (in progress)
status: in_progress
intent: user wants the SDK "very in tune with lean-oracle-sdk" — curated root + module subpaths,
  each subpath a barrel with defined exports. Also questioned why devnetConfig ships at root (it
  should be a preset, not root surface). Decisions (AskUserQuestion): CURATED ROOT (faithful — low-
  level only on subpaths; update consumers) + subpath set = presets + tx + ckb + oracle.
did: studied lean-oracle-sdk (/home/destiny/ckb/lean_oracle/packages/sdk) — root is curated
  (clients/presets/public types/errors/decode/discovery); ./ckb ./tx ./fuel ./hermes ./presets
  ./advanced are module barrels; package.json#exports maps each to dist/<area>/index.js; it bundles
  testnet+mainnet presets but NOT devnet (devnet is ephemeral). Mapped our analog:
    root (curated): clients, constants/enums, domain types (PoolData/Script/PoolDeployment/Hex/...),
      pool reads (getPool/listPools/balances/poolTypeHashOf/...), pool decode (encode/decodePoolData,
      asPool/asShare/asTreasury), payout (mulDivFloor/redeemPayout).
    ./presets: devnetConfig + DEVNET_*, definePoolNetworkConfig, configForPoolTypeVersion,
      to{PoolDeployment,PoolCodeDeps}, config/network types. (devnet MOVES off root.)
    ./tx: build* + initiate* + params, completeFeeAndChange, attach*, OracleTick/assertTickForPool,
      asset resolution.
    ./ckb: scripts, computeTypeId, amount codec, oracleCommit, createClient/createPrivateKeySigner,
      bytesToHex/hexToBytes, Network/DevnetSecpOverride.
    ./oracle: unchanged.
  Blast radius: watcher imports everything from root today; only 4 files need updating
  (bin/server.ts, config.ts, oracle/source.ts, executor.ts) — rest use root-curated names only.
  SDK test fixtures import build*/scripts from root too -> will repoint to subpaths (validates them).
files: (planning only so far)
decisions: devnet stays bundled but as a PRESET under ./presets (not root) — it's the least-stable
  network so it does NOT belong on the curated root; testnet/mainnet presets to be added there when
  deployed. No /advanced or /query subpath for now (per user).
gotchas: tsc rootDir=src/outDir=dist already emits dist/<area>/index.js, so no tsconfig change —
  just add the barrels + package.json exports. Curated root WILL break any consumer importing a
  low-level name from root (watcher 4 files + SDK tests) — must update those.
next: create src/ckb/index.ts + src/tx/index.ts barrels; rewrite src/index.ts curated; update
  package.json exports; fix the 4 watcher files + test imports; build both + run tests.

## SDK re-layout into subpaths — DONE
status: done
intent: (continuation) finish the curated-root + presets/tx/ckb/oracle subpath restructure.
did: Created src/ckb/index.ts + src/tx/index.ts barrels; updated src/presets/index.ts header
  (+ re-export Network); rewrote src/index.ts to the curated surface (clients, constants, domain
  types, Hex, PoolData decode, payout, reads ONLY). Added ./presets ./tx ./ckb to package.json
  exports (oracle already there); tsc emits dist/<area>/index.js with no tsconfig change. Repointed
  all SDK test fixtures (build*/scripts/codecs/etc.) to the subpaths; 90/90 still pass. Updated the
  4 watcher files (bin/server.ts, config.ts, oracle/source.ts, executor.ts) to import low-level
  names from the right subpaths — watcher typecheck clean + 55/55 tests pass. Web imports no SDK
  (no-SDK rule) — unaffected. Updated README: fixed the stale "DEPOSIT reads a header dep" line
  (now CREATE+CLOSE only; DEPOSIT/WITHDRAW status-gated), added an "Entry points" table documenting
  the curated root + 4 subpaths, added WITHDRAW to the transition list + client description.
  Runtime-verified: root no longer leaks build*/scripts/devnetConfig/oracleCommit; each subpath
  exposes its names.
files: packages/game-sdk/src/index.ts (curated); src/ckb/index.ts (new); src/tx/index.ts (new);
  src/presets/index.ts; package.json (exports); README.md;
  tests/{assetResolve,cells,clientApi,poolData,scripts,oracleCommit,txBuilders,txKeeper,
  txPlumbing,txSettle}.fixture.mjs; packages/watcher/src/{bin/server,config,oracle/source,executor}.ts
decisions: devnet moved OFF root to ./presets — it's the least-stable network, doesn't belong on a
  curated surface (answers the user's "why import devnetConfig" — it shouldn't be a root export).
  Mirrors lean-oracle which bundles testnet/mainnet (stable) and NOT devnet. Kept reads + PoolData
  decode at root (lean-oracle keeps basic decode/discovery at root). No /advanced or /query (per user).
  Network re-exported from both ./ckb (definition) and ./presets (config surface).
gotchas: any FUTURE consumer importing a builder/script/devnetConfig from the bare root will now
  break — that's intentional (curated root). The 4 SDK code-version hashes in presets/devnet.ts are
  still a snapshot behind HEAD (untouched). README internal "Layout" section still lists src/ folders
  (fine; the new "Entry points" table is the public-surface doc).
next: optional — add bundled testnet/mainnet presets to ./presets once those deploy; consider a
  /tx/withdraw watcher endpoint so the frontend can reach withdraw; the getShareBalances
  query-by-holder efficiency fix is still open.

## Parity tweaks: one deployment object + ScriptLike
status: done
intent: close the two ergonomics gaps vs lean-oracle found in the args comparison — (1) `deploy` +
  `deps` as separate workflow fields, (2) strict `Script` where lean-oracle takes CCC `ScriptLike`.
did: (1) Replaced `deploy: PoolDeployment; deps: PoolCodeDeps;` with a single `deployment:
  PoolDeploymentConfig;` on all 6 client-resolving workflow param interfaces (InitiateDeposit/
  Withdraw/Redeem/BurnShares/Close/KeeperTransition). Added `deploymentViews()` helper in
  workflows.ts that splits it into deploy+deps via toPoolDeployment/toPoolCodeDeps for the pure
  builders. Updated PlayerClient/KeeperClient draft* + keeperParams to pass
  `deployment: this.config.deployment` (was deploy+deps); DraftArgs Omit "client"|"deployment".
  (2) Widened caller locks to `ccc.ScriptLike`: workflow depositorLock/withdrawerLock/redeemerLock/
  holderLock/creatorLock (normalized once at fn top via ccc.Script.from), DraftCreateParams.creatorLock
  (normalized before buildCreatePoolTx; KeeperClient now imports ccc as value), and the read layer
  (getShareBalances/collectShareCells/collectAssetCells holderLock + PoolReaderClient.getShareBalances).
  Pure build* params left strict (advanced layer; ccc.Script satisfies them structurally). consumer-
  facing draft* arg shape unchanged -> watcher needed NO edits.
files: packages/game-sdk/src/tx/workflows.ts (rewrite); src/client/PlayerClient.ts;
  src/client/KeeperClient.ts; src/client/PoolReaderClient.ts; src/query/pools.ts
decisions: workflows take `deployment: PoolDeploymentConfig` (not full PoolNetworkConfig) — exactly
  the deploy+deps source, no unused rpc/name. Kept the pure build* layer on strict `Script` to bound
  scope; the boundary (workflows/clients/reads) is where wallet locks enter, so that's where
  ScriptLike matters.
gotchas: SDK build clean, 90/90; watcher typecheck + 55/55. No test changes needed (no test calls
  initiate* directly; clients' external arg shape unchanged). PoolReaderClient/KeeperClient may now
  have an unused `type Script` import — harmless (no noUnusedLocals).
next: answer the user's strategic "why an SDK" question (discussion, not code).

## SDK packaging sketch — Player(public) vs Keeper(internal)
status: done
intent: user asked for a concrete sketch of a segmented public package after the "why an SDK"
  discussion (rake → pool creator on CLOSE; player surface drives our rake, keeper surface is the
  operator playbook a competitor could reuse).
did: Wrote docs/sdk-packaging-sketch.md. Bucketed every current export into PUBLIC-player /
  PUBLIC-reads / PUBLIC-neutral-primitives / INTERNAL-keeper. Target = TWO packages:
  `ckb-up-down-sdk` (published; player+reads+neutral+presets, NO oracleCommit/create/lifecycle) +
  `ckb-up-down-operator` (internal; KeeperClient + create/lifecycle/close builders+workflows +
  oracle adapter + oracleCommit, depends on + re-exports the public one). Listed the per-package
  exports maps + the keeper-only source files to move (create/close/keeperTransitions/KeeperClient/
  oracle/oracleCommit; split asset.ts/oracleTick.ts/workflows.ts). Documented a lightweight INTERIM
  (one package, omit ./keeper+./oracle from published exports map — Node blocks unlisted subpaths;
  friction not secrecy since JS still in tarball). Noted frontend unaffected (no-SDK rule; public
  package audience is external integrators), watcher depends on operator pkg / workspace link.
files: docs/sdk-packaging-sketch.md (new)
decisions: RECOMMEND NOT NOW — keep SDK internal until there's a reason to court integrators; do the
  two-package split then (only option that truly omits operator code from the public artifact). The
  role-split clients + subpaths make it a packaging job, not a rewrite. Doc is plan-of-record.
gotchas: this is a sketch only — no package split implemented. `exports`-gating (interim) hides the
  import specifier but still ships keeper JS in the tarball; only two-package removes it.
next: (when courting integrators) implement the two-package split per the doc. Otherwise the SDK
  follow-ons remain: testnet/mainnet presets, /tx/withdraw watcher endpoint, getShareBalances
  query-by-holder efficiency fix.

## getShareBalances query-by-holder efficiency fix
status: done
intent: getShareBalances/collectShareCells scanned the pool's ENTIRE share supply (query by share
  type, filter by lock client-side) — O(all holders) for one holder's answer. Flip to query the
  holder's own cells.
did: Added private `holderShareCells()` in query/pools.ts — ONE lock-scoped findCells
  (scriptType:"lock", exact) over the holder's cells, filtered to this pool's shares via the
  existing `asShare` classifier (returns {outPoint, side, amount}). Rewrote getShareBalances
  (tally up/down from it — also collapses the old 2 type-queries into 1) and collectShareCells
  (filter to the requested side). Mirrors how collectAssetCells already worked. listShareCells
  (whole-pool indexing) deliberately LEFT type-scoped (it wants every holder). SDK 90/90.
files: packages/game-sdk/src/query/pools.ts
decisions: scans holder cells (few for a normal wallet) not pool cells (many for a popular pool) —
  the right shape for the positions/redeem/withdraw hot path. Possible loss only for a holder with a
  huge cell count and few shares (rare); net win. No unit test mocks findCells here (devnet-exercised),
  logic mirrors proven collectAssetCells.
gotchas: bytesToHex/concatBytes/hexToFixed imports still used by listShareCells — no dangling refs.

## /tx/withdraw watcher endpoint
status: done
intent: expose WITHDRAW through the watcher so the frontend (no-SDK rule) can reach it, mirroring
  POST /tx/deposit.
did: Added `withdraw` to PoolTxBuilder + createTxBuilder in api/txBuilder.ts (draftWithdraw ->
  finalize via the key-less readonly signer, identical shape to deposit). Added POST /tx/withdraw
  route in api/server.ts (same validation as deposit: poolId + lock + up/down, reject zero stake).
  Extended tests/api.fixture.mjs: withdraw in fakeBuilder + 2 new tests (forward intent/amounts;
  reject zero+missing). Watcher typecheck clean + 57/57 (was 55). Web mock already stubs all /tx/*
  generically, so /tx/withdraw works in the demo with no mock change; real watcher returns a real
  unsigned tx.
files: packages/watcher/src/api/txBuilder.ts; packages/watcher/src/api/server.ts;
  packages/watcher/tests/api.fixture.mjs
decisions: none beyond mirroring deposit. Frontend withdraw UI (button + POST) is the remaining
  consumer-side piece, out of scope here.
next: testnet/mainnet presets remain BLOCKED on an actual deployment. Optional frontend: a withdraw
  action in pool detail that POSTs /tx/withdraw. Two-package SDK split per docs/sdk-packaging-sketch.md
  only when courting integrators.

## Read-endpoint efficiency pass: operator-pool listing + listShareCells
status: done
intent: user — audit each read endpoint for efficiency; concretely, the SDK should store the
  operator/"worker" lock hashes so it can fetch only OUR pools instead of scanning the whole
  (permissionless) deployment.
did: Audited all reads. Two changes:
  (1) listPools was a prefix scan over EVERY pool_type cell (incl. pools anyone else created on the
  same contracts — efficiency AND correctness bug: the indexer would index/serve pools we can't
  manage or earn rake on). Added `listPoolsByCreator(client, deploy, creatorLockHash)` — a
  LOCK-scoped search on the PoolCell's pool_admin_lock (args == creatorLockHash), returning only our
  pools. Added `operatorLockHashes?: Hex[]` to PoolNetworkConfig (operator identity, caller-supplied,
  NOT deployment) + `PoolReaderClient.listManagedPools()` (union over them, de-dup by poolId, throws
  if unset) + `listPoolsByCreator()` passthrough. Exported listPoolsByCreator from the curated root.
  Wired the watcher: IndexContext gains `creatorLockHash`; service.ts computes
  `ccc.Script.from(creatorLock).hash()` (value-imported ccc) and passes it; indexOnce now lists via
  listPoolsByCreator. Factored a `cellToPoolView` helper (listPools + listPoolsByCreator share it).
  (2) listShareCells did 2 exact queries (one per side); replaced with ONE prefix query on
  `args == poolTypeHash` (both sides; asShare reads the side back) — halves queries in the indexer's
  per-pool hot loop. Dropped now-unused concatBytes import.
  Added clientApi test (listManagedPools throws unset; queries pool_admin_lock per hash). SDK 91/91;
  watcher typecheck + 57/57.
files: packages/game-sdk/src/query/pools.ts; src/presets/config.ts; src/client/PoolReaderClient.ts;
  src/index.ts; tests/clientApi.fixture.mjs; packages/watcher/src/indexer.ts; src/service.ts
decisions: listPools kept as the explicit "ALL pools" primitive (doc warns it's deployment-wide);
  operator filtering is opt-in via listPoolsByCreator/listManagedPools. operatorLockHashes is a LIST
  (key rotation / multiple keepers). Watcher uses its existing config.creatorLock (single operator)
  via the primitive directly — didn't route it through config.operatorLockHashes (the watcher
  already "stores" creatorLock).
gotchas: getPool/getPoolByTypeScript (exact typeID), getTreasuryBalance (exact treasury lock),
  collectAssetCells (holder lock + early break) were already optimal — unchanged. The indexer's lane-
  feed filter STAYS (second narrowing among our pools). indexer.fixture tests pure projection helpers,
  not indexOnce, so the query swap needed no test change.
next: none open on reads. Remaining session-006 follow-ons unchanged (testnet/mainnet presets blocked;
  frontend withdraw button; devnet preset refresh after redeploy; two-package split when courting integrators).

## Query-structure refactor (4 changes, user-directed)
status: done
intent: user audit of the query layer — pool-key the share/treasury reads (read shareXudtCodeHash
  from pool data, not deploy default); drop redundant getPoolByTypeScript; add a filter to listPools;
  store operator lock hashes in presets.
did:
  (1) POOL-KEYED READS (correctness fix): getShareBalances/listShareCells/getShareSupply/
  getTreasuryBalance now take a `PoolView` and derive poolTypeHash = hash(typeScript) AND
  shareXudtCodeHash/assetTypeHash/treasuryLockCodeHash from the POOL's own data — was using
  deploy.shareXudtCodeHash (the deployment default) everywhere, which silently misreads any pool that
  pinned a non-default share code. getTreasuryBalance throws for CKB pools. collectShareCells stays an
  explicit-hash PRIMITIVE (burn-after-close has no PoolView). Workflow callers (withdraw/redeem) now
  pass pool.data.shareXudtCodeHash; burn keeps deploy default (pool may be gone). Client methods
  getShareBalances(poolId)/getTreasuryBalance(poolId) fetch the pool first (requirePool helper).
  (2) DROPPED getPoolByTypeScript export — inlined into getPool (it was only used internally; a pool's
  type script is fully determined by poolId+deploy, so it added nothing).
  (3) listPools(client, deploy, filter?) with PoolFilter `{ creator?, status?, feedId? }` — `creator`
  switches to the lock-scoped pool_admin_lock search (folds in the old listPoolsByCreator, now removed),
  status/feedId post-decode. listManagedPools + the watcher indexer use listPools({creator}). Indexer
  also dropped IndexContext.shareXudtCodeHash (listShareCells(pool) derives it) + service stopped passing it.
  (4) devnetConfig gained `operatorLockHashes?` option (threaded into the config) — "save operator
  identity with the preset"; for devnet it's caller-passed (per-machine), bundled for testnet/mainnet later.
  Exported PoolFilter; removed getPoolByTypeScript + listPoolsByCreator from index. Updated the devnet
  lifecycle integration test (getShareBalances(poolId,...)) + README query bullet. SDK 91/91; watcher 57/57.
files: packages/game-sdk/src/query/pools.ts; src/client/PoolReaderClient.ts; src/tx/workflows.ts;
  src/presets/devnet.ts; src/index.ts; README.md; tests/integration/devnet/lifecycle.test.mjs;
  packages/watcher/src/indexer.ts; src/service.ts
decisions: two-tier shares — PoolView-keyed public reads (correct by construction) + explicit-hash
  primitive (collectShareCells) for the pool-absent burn path. getPool is the ONLY single-pool fetch.
  Per-cadence creatorLock (one-worker-one-cadence) is a WATCHER LaneConfig concern, NOT changed here;
  the SDK already unions N operator hashes via listManagedPools.
gotchas: client getShareBalances/getTreasuryBalance now do an extra getPool round-trip (acceptable for
  ad-hoc reads; standalone PoolView forms avoid it for callers that already hold the pool). listPools
  status/feedId are in-memory filters (only `creator` narrows the on-chain query).
next: per-cadence creatorLock in the watcher LaneConfig if going one-key-per-cadence (separate, watcher-
  side). testnet/mainnet preset builders carry operatorLockHashes when those deploy.
